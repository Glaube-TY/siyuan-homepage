import { WIDGET_PRESENTATION_CONTRACT_VERSION, type WidgetPresentationManifest } from "../../../widgetPresentation/types";

export const handDrawnWidgetPresentation: WidgetPresentationManifest = Object.freeze({
    contractVersion: WIDGET_PRESENTATION_CONTRACT_VERSION,
    shell: Object.freeze({
        id: "hand-drawn.sketch-card",
        variants: 4,
        tokens: Object.freeze({
            background: "var(--hp-sketch-card)",
            border: "0",
            borderRadius: "0",
            boxShadow: "none",
        }),
    }),
    generic: Object.freeze({ id: "hand-drawn.workspace.generic" }),
    categories: Object.freeze({
        collection: Object.freeze({ id: "hand-drawn.workspace.list" }),
        metrics: Object.freeze({ id: "hand-drawn.workspace.stat" }),
        visualization: Object.freeze({ id: "hand-drawn.workspace.chart" }),
        editorial: Object.freeze({ id: "hand-drawn.workspace.note" }),
        media: Object.freeze({ id: "hand-drawn.workspace.media" }),
        control: Object.freeze({ id: "hand-drawn.workspace.utility" }),
        embedded: Object.freeze({ id: "hand-drawn.workspace.embed" }),
        workspace: Object.freeze({ id: "hand-drawn.workspace.complex" }),
        intrinsic: Object.freeze({ id: "hand-drawn.workspace.custom" }),
    }),
    kinds: Object.freeze({
        list: Object.freeze({ id: "hand-drawn.workspace.list" }),
        task: Object.freeze({ id: "hand-drawn.workspace.task" }),
        stat: Object.freeze({ id: "hand-drawn.workspace.stat" }),
        chart: Object.freeze({ id: "hand-drawn.workspace.chart" }),
        calendar: Object.freeze({ id: "hand-drawn.workspace.calendar" }),
        note: Object.freeze({ id: "hand-drawn.workspace.note" }),
        media: Object.freeze({ id: "hand-drawn.workspace.media" }),
        utility: Object.freeze({ id: "hand-drawn.workspace.utility" }),
        embed: Object.freeze({ id: "hand-drawn.workspace.embed" }),
        complex: Object.freeze({ id: "hand-drawn.workspace.complex" }),
        custom: Object.freeze({ id: "hand-drawn.workspace.custom" }),
    }),
    widgets: Object.freeze({
        "latest-docs": Object.freeze({ id: "hand-drawn.workspace.latest-docs", scope: "full" }),
        favorites: Object.freeze({ id: "hand-drawn.workspace.favorites", scope: "full" }),
        "recent-journals": Object.freeze({ id: "hand-drawn.workspace.recent-journals", scope: "full" }),
        TaskMan: Object.freeze({ id: "hand-drawn.workspace.taskman", scope: "full" }),
        HOT: Object.freeze({ id: "hand-drawn.workspace.hot", scope: "full" }),
        childDocs: Object.freeze({ id: "hand-drawn.workspace.child-docs", scope: "full" }),
        conditionDocs: Object.freeze({ id: "hand-drawn.workspace.condition-docs", scope: "full" }),
        "quick-notes": Object.freeze({ id: "hand-drawn.workspace.quick-notes", scope: "full" }),
        TaskManPlus: Object.freeze({ id: "hand-drawn.workspace.taskman-plus", scope: "full" }),
        sql: Object.freeze({ id: "hand-drawn.workspace.sql", scope: "chrome" }),
        constellation: Object.freeze({ id: "hand-drawn.workspace.constellation", scope: "chrome" }),
        reviewDocs: Object.freeze({ id: "hand-drawn.workspace.review-docs", scope: "chrome" }),
        fixedAssets: Object.freeze({ id: "hand-drawn.workspace.fixed-assets", scope: "chrome" }),
        enhancedDiary: Object.freeze({ id: "hand-drawn.workspace.enhanced-diary", scope: "native" }),
        weather: Object.freeze({ id: "hand-drawn.workspace.native.weather", scope: "native" }),
        stikynot: Object.freeze({ id: "hand-drawn.workspace.native.stikynot", scope: "native" }),
        statisticalCard: Object.freeze({ id: "hand-drawn.workspace.native.statistical-card", scope: "native" }),
        almanac: Object.freeze({ id: "hand-drawn.workspace.native.almanac", scope: "native" }),
        PicCaro: Object.freeze({ id: "hand-drawn.workspace.native.pic-caro", scope: "native" }),
    }),
    icons: Object.freeze({
        "documents.recent": "iconFile",
        "documents.favorite": "iconBookmark",
        "journal.recent": "iconCalendar",
        "task.list": "iconCheck",
        documents: "iconFile",
        tasks: "iconCheck",
        list: "iconList",
        stat: "iconGraph",
        chart: "iconGraph",
        calendar: "iconCalendar",
        note: "iconEdit",
        media: "iconRecord",
        utility: "iconTools",
        embed: "iconLink",
        complex: "iconWorkspace",
    }),
});
