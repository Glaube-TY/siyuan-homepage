# 主页插件大版本产品方向与功能路线（2026-08-12）

本文记录已经确认的产品方向、明确暂缓/不考虑的事项，以及仍需后续讨论的候选。记录目标不是罗列功能名称，而是保存每项功能想解决的问题、预期体验、实现边界和与现有架构的关系，避免后续开发逐渐偏离原始目标。

后续设计和开发必须遵守项目现有的注册入口、设备视图边界、主题框架、组件样式框架和数据服务边界。新增功能应尽量注册到已有体系，不得因为单个功能建立一套平行且孤立的配置、样式或业务数据实现。

## 优先级定义

- **P0｜当前大版本主线**：本次大版本发布前后的主要架构与体验优化。
- **P1｜明确要做的核心能力**：方向已经确认，应作为后续连续开发的重点；具体实现仍可分阶段讨论。
- **P2｜明确保留、逐步推进**：确定有价值，但依赖前置架构、外部数据源或需要逐组件建设。
- **暂缓/不考虑**：当前明确不作为开发方向；除非出现新的高价值场景，否则不重新启动。
- **建议候选**：由本轮分析提出，尚未由产品方向确认。

## 路线总览

| 优先级 | 方向 | 当前决策 |
| --- | --- | --- |
| P0 | 设置全局搜索、作用域与轻量保存状态 | 已完成，用户验收通过 |
| P0 | 统一 Agent 平台：共享 Kernel、Profile、能力注册、上下文、恢复与评测 | 当前连续开发主线；从底层契约开始逐层迁移，见下方“Agent 主线总览” |
| P0 | 组件呈现类别与展示变体契约 | 已完成，用户验收通过 |
| P0 | 全局性能优化 | 已完成 |
| P1 | 统一 Agent 平台上层能力：临时工作台、进化记忆、主页智能入口、定时/心跳 | 明确要做；必须建立在统一 Agent 平台上，不再单独建设执行框架 |
| P1 | 强化日记工作台作为底层承载，主页负责摘要与入口 | 明确认可，后续讨论实现 |
| P1 | 高度自定义的全局日历组件 | 明确要做 |
| P1 | 习惯打卡系统与主页组件 | 明确要做 |
| P1 | 高级番茄钟与专注数据中心 | 明确要做 |
| P2 | 数据库数据转图表组件重构并重新开放 | 明确要做，已有隐藏基础 |
| P2 | 文献阅读数据组件与思源同步 | 明确要做，不以 Agent MCP 形态接入 |
| P2 | 内容对象右键动作 | 可以做，按组件逐步接入 |
| 暂缓 | 插件自建全局命令面板 | 不做；必要时注册到思源命令体系 |
| 暂缓 | 桌面端布局编辑模式增强 | 当前不做，已有布局模板 |
| 不考虑 | 单独建设全局快速预览与就地处理体系 | 现有预览框已内置编辑器，新增价值不足 |
| 暂缓 | 以用户手工配置为核心的跨组件规则中心 | 不作为主要方向，优先 AI Agent 自动化 |
| 暂缓 | 上下文镜头/场景过滤系统 | 配置成本高，实际受众有限 |
| 未来候选 | LLM Wiki / AI 知识管家 | 从本次大版本路线取消；等插件稳定、思源内核插件与跨端数据能力成熟后重新评估 |

### Agent 主线总览（当前连续开发主线）

路线总览中原先分散的“Agent 执行状态”“内核对标”“上下文治理”“记忆”“主页接入”“临时工作台”和“定时/心跳”，现在统一归入一条 **Agent 平台主线**。它们不是若干互不相关的功能，而是同一个底层框架从内核到产品入口的不同阶段。

#### 不可偏离的总体目标

这条主线的最终目标不是继续堆叠若干彼此独立的 AI 功能，而是建设一套可供整个主页复用的 Agent 平台：知识库对话作为全能力入口，机器人、状态语、划词、主页卡片和后台任务通过版本化 Profile 按需装配同一套底层能力。任何新入口都不得重新实现模型请求、Agent 循环、工具调度、上下文压缩、权限、错误恢复或 trace。

平台长期保持以下边界：

1. `NativeToolAgentLoop` 保持为单 Agent 原生工具调用内核；除非评测证明无法满足需求，否则不替换为另一个 Agent 框架，也不建设平行循环。
2. Profile 只声明一个入口真正需要的能力和策略；状态语等轻任务不被强行包装成完整 Agent。
3. 模型决策、上下文装配、能力注册、权限策略、副作用执行、运行记录和 UI 投影分层，但只有出现真实复用点时才抽接口，不为未来假设创建空壳架构。
4. 一次执行以标准 `session/run/step/event` 协议作为事实来源；聊天消息、恢复提示、诊断面板和执行状态 UI 都从该协议派生，不能各自猜测运行状态。
5. 上下文目标是“最小且高信号”，不是无限携带原文；不可压缩约束、当前工作状态、近期原文、分层摘要和按需检索必须有明确边界。
6. 写操作以真实最终状态为准。结果未知时先回读验证，禁止无条件重放；模型声称“已经完成”不能代替环境状态验证。
7. 每次底层改造必须先有可重复的 Agent eval 或专项验证，验收后再迁移下一个入口；不以“构建通过”代替行为正确。
8. 本次属于大版本重构，不保留旧 Agent 会话、旧 trace、旧检查点和旧上下文摘要的数据兼容层。结构变化导致旧聊天无法继续时允许直接失效，但不得借此删除非 Agent 业务数据、当前设置、安全校验和写后验证。

#### 目标运行结构

```text
知识库 / 机器人 / 状态语 / 划词 / 主页 / 后台任务
                         ↓
              Agent Profile 注册表
                         ↓
                 统一运行组装入口
      ┌──────────┬──────────┬──────────┐
      │模型协议  │上下文装配│能力与权限│
      └──────────┴──────────┴──────────┘
                         ↓
               NativeToolAgentLoop
                         ↓
           标准 Run / Step / Event 记录
                         ↓
       UI、机器人消息、恢复、诊断与后台投影
```

标准身份层级固定为：

- `sessionId`：一条长期会话或后台任务线程。
- `runId`：一次用户提交或一次后台触发形成的完整执行。
- `stepId` / `stepIndex`：一次模型请求或一个工具执行步骤。
- `eventId`：流式文本、工具、审批、用量、错误、检查点和结束事件。
- `correlationId`：串联 UI、Provider 请求、工具执行、日志和恢复记录。

所有入口最终共享以上语义，但不要求共享同一种界面。

| 阶段 | 建设内容 | 状态与验收边界 |
| --- | --- | --- |
| A0 | 核对现有共享内核、Workbench、机器人和轻量生成入口的边界；收拢路线图 | 已完成；确认现有 `NativeToolAgentLoop` 和 Provider 工厂应被复用，不另写平行 Agent |
| A1 | 建立带版本号的 `AgentProfile`、能力标识和统一注册入口；知识库对话先注册为全能力 Profile | 已完成；Profile 已实际控制 Workbench 的能力装配，现有设置继续在允许范围内细分开关，行为不变 |
| A2 | 建立 Agent eval 基线，并统一模型协议、运行身份、Provider 能力协商、用量、错误、取消/超时和 trace | 已完成；每次知识库 Agent 执行已有统一身份、模型步骤、token 用量、共享请求超时、结构化 Provider 错误和关联 trace |
| A3 | 将上下文、工具、知识、记忆、Skills、MCP 收敛为可组合能力，并建立 Profile 最小权限装配 | 已完成；Profile 已实际约束上下文、工具/action、记忆读写、Skills 与 MCP 范围，模型清单、系统提示和真实执行共用同一授权结果 |
| A4 | 将知识库对话完整迁移为全能力 Profile 与统一运行组装入口 | 已完成；平台装配集中到 `runAgentProfile`，知识库入口只保留领域结果整理，现有能力不减少 |
| A5 | 建立统一上下文账本、分层压缩、持久化检查点和安全恢复 | 已完成；知识库历史统一由会话账本生成，run 级检查点可从安全边界续跑，未知写入结果禁止重放 |
| A6 | 将机器人、状态语和划词 AI 注册为远程受限或轻量 Profile | 已完成；三类入口共用 Profile 权限与模型运行边界，原有界面、机器人 Kernel 工具和取消交互保持不变 |
| A7 | 重构 Agent 执行状态与恢复 UI | 已完成；运行阶段由统一事件投影，崩溃后只允许从安全检查点继续 |
| A8 | 接入主页 Agent 能力注册、临时工作台、进化记忆、定时/心跳和后台 Job | 开发中；A8.1、A8.2、A8.4 与 A8.6.1 已完成，A8.3 已跳过，A8.5 已取消，下一步为 A8.6.2 Scheduler |

当前执行 A8.6 后台 Job；A8.6.1 的契约与存储已经完成，下一步进入 A8.6.2 统一 Scheduler。每个子阶段单独验证、单独提交、单独验收；前一阶段没有通过，不提前叠加后一阶段的大范围改动。

#### 分阶段开发清单

##### A2：统一模型与运行基础协议

1. 先以历史真实故障建立最小 eval 基线，至少覆盖正常回答、原生工具续写、空响应、输出截断、用户取消、请求超时、Provider 限流和用量累计。
2. 在现有 Provider Adapter 上统一能力描述，不增加第二套 ModelGateway；明确流式、原生工具、强制工具选择、并行工具和用量上报能力。
3. 为每轮建立稳定的 `sessionId/runId/correlationId`，让 Workbench 事件、trace、错误和机器人运行能够关联到同一次执行。
4. 统一 Provider 错误类别、`retryable`、`retryAfterMs`、`safeToReplay`、`sideEffectState` 和用户下一步；本阶段只建立协议，不提前实现 A5 的复杂自动恢复。
5. 统一模型请求超时和外部取消，严格区分 `provider_timeout` 与 `user_aborted`，并确保流式和非流式传输都能结束。
6. 汇总每个模型步骤的输入、输出、缓存和推理 token，写入运行结果与 trace；Provider 无法上报时明确标记未知，不伪造数字。
7. 验收：现有知识库与机器人行为不变；相同 run 的事件 ID 一致；超时不会伪装成用户取消；用量不会在循环中丢失；专项脚本、类型检查和生产构建通过。

完成记录（2026-08-12）：

- 已建立 `AgentRunIdentity` 和稳定事件 ID，知识库 Workbench 事件与 trace 共享 `sessionId/runId/correlationId`。
- 已直接扩展现有 Provider Adapter 能力协议，没有新增第二套 ModelGateway；流式、原生工具、强制工具、并行工具和用量上报均可诊断。
- 已统一 OpenAI 兼容、Gemini 和 Anthropic 用量格式，并按模型步骤累计输入、输出、缓存和推理 token。
- 已建立浏览器与 Kernel 共用的模型请求超时传输，删除机器人私有的重复超时包装，并保持机器人整轮超时与权限逻辑不变。
- 已扩展 Provider 错误的类别、是否可重试、建议等待、是否可安全重放、副作用状态和用户下一步，并写入安全 trace。
- 已修复 Provider 空响应被误判为成功的问题；空响应、输出截断、用户取消和请求超时现在有不同终止语义。
- 已新增 `verify:agent-runtime`，并通过 Agent Profile、运行协议、续写保护、TypeScript 全量检查和应用/Kernel 生产构建。

##### A3：可组合能力与最小权限装配

1. 复用现有上下文 Builder、工具 Registry、全局记忆、知识检索、Skills 和 MCP 实现，将它们接到统一装配入口，不重写业务逻辑。
2. Profile 从粗粒度能力名继续收敛到明确的上下文来源、工具/action 白名单、记忆读写、Skills/MCP 范围和执行限制。
3. 有效权限取“平台硬限制、Profile 白名单、用户设置、当前运行环境、单次审批”的交集；任何外部 MCP 注解只作为提示，不能覆盖本地策略。
4. 验收：禁用能力不会注册、不会进入提示词、也无法通过聚合工具绕过；全能力知识库 Profile 的现有工具集合保持一致。

完成记录：

- `AgentProfile` 升级为版本 2，明确声明上下文来源、工具名、聚合 action、全局记忆读写、外部 Skill ID、MCP Server/工具和最大工具调用次数。
- `ToolRegistry` 成为工具授权的共同边界：未授权工具不注册；未授权聚合 action 同时从 Provider Schema、`agent_tool_help` 和执行入口移除，直接伪造调用也会返回 `permission_denied`。
- 全局记忆读取与写入已拆分授权；新版记忆中枢按 Profile 决定入口权限，并与记忆中枢总开关和“允许 AI 自动学习”设置求交。
- 对话历史、知识规则、附加文档正文、Skill 指令、外部 Skill 索引和运行时工具信息已按 Profile 的上下文来源选择性注入；禁止读取全局记忆时也会清除会话快照中的记忆副本。
- 外部 Skill 与 MCP 的资源白名单会继续与用户禁用项求交；MCP 列表、说明、同步、调用、权限预览和配置写入使用同一 Server/工具授权判断。
- 系统提示只描述本轮真实可用的工具/action，不再引导模型调用被 Profile 或当前设置移除的能力。
- 全能力知识库 Profile 保持全部现有能力；新增校验覆盖受限 Profile、资源范围、聚合 action Schema/帮助/执行隔离和提示词隔离，并通过 Agent 运行协议、续写保护和 TypeScript 检查。

##### A4：知识库全能力 Profile 完整迁移

1. 将 `runAgentTurn` 中模型、上下文、权限、工具、记忆、Skills、MCP、恢复和 trace 的组装收敛到统一运行入口。
2. Workbench 只保留知识库领域参数和 UI 回调，不再承担平台级装配职责。
3. 删除已由统一入口取代的专用转发函数和重复分支；旧 Agent 会话结构不做迁移。
4. 验收：知识库现有工具、引用、流式推理、确认、全局记忆、MCP、Skills 和设置行为全部通过回归，主流程职责显著减少。

完成记录（2026-08-12）：

- 新增统一 `runAgentProfile` 运行入口，集中完成 Profile、模型、上下文、权限、工具、记忆、Skills、MCP、确认、事件、trace 和流式 Agent 循环的装配。
- `runAgentTurn` 已收敛为知识库领域适配器，只选择全能力知识库 Profile，并负责引用校验、引用分段和阶段摘要等领域结果整理。
- 删除仅做参数转发的原生 Agent Runner 与工具 Registry Builder，直接复用现有 `NativeToolAgentLoop` 和 Workbench Registry 工厂，没有增加第二套 Kernel。
- 知识库 UI 和会话流程继续调用原入口，确认、工具事件、流式输出、全局记忆、Skills、MCP 与设置行为保持原有契约。
- Agent 平台校验新增结构边界约束，确保 Provider、Workbench、循环、记忆和 MCP 装配不会重新回流到知识库适配器。

##### A5：长上下文、检查点与安全恢复

1. 统一聊天历史和 AgentSession 的事实来源，避免两套压缩历史产生分歧。
2. 建立不可压缩约束、当前工作状态、近期原文、带覆盖范围的分层摘要和按需检索五层上下文。
3. 每次请求生成 Context Manifest，记录来源、字符/token、是否注入、排除原因和摘要覆盖范围；优先使用 Provider 实际用量。
4. 在模型前、工具前、工具结果后、等待审批和最终结束处保存按 run 隔离的检查点。
5. 写操作结果未知时调用对应回读验证器；不能证明安全时停止并交由用户决定，绝不盲目重放。
6. 成功结束必须同时满足 Provider 完成、无待执行工具、无待审批、无未知副作用且输出契约通过；“下一步”文本检测只保留为兜底。
7. 验收：10/30/100 轮事实记忆、后续修正、未完成步骤、压缩后回读和不同会话隔离通过；刷新或断流后可从安全边界继续且不重复写入。

完成记录（2026-08-12）：

- UI 会话、阶段摘要与压缩摘要成为知识库历史的唯一事实来源；删除长期持久化的知识库 `agentSession` 及其删除、重生成和存储同步分支，避免两套历史和两套压缩结果分歧。
- `AgentSession` 收敛为单个 run 内的追加日志：历史事实通过 `ConversationContextSnapshot` 注入，工具调用与工具结果只在当前 run 保留；未结束 run 才进入脱敏检查点。
- 建立五层上下文账本：当前约束、活动工作状态、近期原文、历史摘要和按需检索索引；旧轮次由带覆盖范围的压缩摘要承载，最近十轮保留原文，可信资源 ID 继续支持按需回读。
- 每次请求生成 Context Manifest，记录当前问题、压缩历史、近期轮次、工作目标、全局记忆、附加文档、知识规则、Skills、运行时工具和工具清单的注入状态、字符数、估算 token、排除原因及摘要覆盖轮次；trace 同时保留 Provider 实际 token 用量，实际值优先于估算。
- 原有双写中断日志升级为 run 检查点，在模型前、工具前、等待确认、工具结果后和最终结束记录稳定 `sessionId/runId/correlationId`、脱敏消息、步骤、待执行工具和副作用状态。
- 统一运行入口支持从 `before_model` 与结果已知的 `after_tool` 检查点继续，并保持原 run 身份；待执行工具、等待确认、已结束、会话不匹配和副作用未知的检查点一律拒绝续跑。
- 写工具在返回前抛错会统一标记 `write_result_unknown`、`safeToReplay=false` 和 `sideEffectState=unknown`，立即停止本轮；必须先回读目标状态，运行时不得盲目重放。
- 检查点会删除 reasoning、敏感字段、正文和本地路径，并压缩工具结果；完成会话持久化后仍沿用现有规则清除，不把 run 日志重新变成长久的第二份会话历史。
- 新增 `verify:agent-context`，覆盖 10/30/100 轮连续性、最新事实修正、压缩覆盖清单、写入结果未知拦截、检查点恢复和恢复时不重复追加用户消息。

##### A6：其他 AI 入口迁移

1. 机器人注册为远程受限 Profile，复用统一模型、上下文、权限、超时、错误、trace 和 Agent Kernel；删除手工重复装配。
2. 状态语注册为单轮轻量 Profile，只读取明确授权的统计事实，不开放工具循环、长期记忆写入或无关上下文。
3. 划词 AI 注册为编辑器轻量 Profile，只获得选区、必要文档上下文和指定编辑能力；关闭弹窗必须取消对应 run。
4. 验收：入口间上下文与权限不串线，原有交互形态不变，重复模型和错误处理逻辑被删除。

完成记录（2026-08-12）：

- 新增远程机器人、主页状态语和编辑器划词三个正式 Profile；远程机器人可读写共享长期记忆，主页状态语只读相关记忆，编辑器划词默认不读取记忆；外部 Skill 与 MCP 继续按入口分别授权。
- 机器人继续复用既有 Provider Adapter、`AgentSession`、`NativeToolAgentLoop`、整轮超时与远程确认桥，不建立第二套 Kernel；Profile 在现有用户工具策略之外增加 Kernel-safe 工具名硬白名单，桌面主页管理、音乐播放、本地命令、网页和 MCP 不会因 Registry 扩展而意外暴露给远程入口。
- 状态语保持原有统计加载、缓存、模型选择、思考模式和会员校验，只通过轻量 Profile 读取用户明确选择的统计事实；不注册工具循环、对话历史或记忆写入。
- 划词 AI 的流式与非流式分支已统一进入共享纯文本模型边界，删除本地重复的流式调用、取消识别和错误转换；只声明选区与技能明确启用的文档上下文，仍由原弹窗展示结果。
- 划词弹窗关闭和组件销毁继续中止当前 `AbortController`，专项校验固定该生命周期，避免关闭后请求继续运行或结果回写到已销毁界面。
- Profile 允许无工具轻任务将工具调用上限声明为 `0`；带工具 Profile 仍必须至少允许一次调用，避免轻量入口为了满足格式被伪装成完整 Agent。
- 扩展 `verify:agent-platform` 覆盖三类 Profile 的上下文、工具、记忆、Skill/MCP 隔离，机器人危险工具排除、统一流式入口和划词关闭取消；并通过 Agent 运行协议、续写保护、长上下文恢复、TypeScript 检查及应用/Kernel 生产构建。

##### A7：执行状态与恢复 UI

1. UI 直接消费标准运行事件，显示准备、模型等待、工具执行、等待确认、压缩、重试、验证、部分完成、失败和取消。
2. 正文只呈现对用户有价值的过程，不把模型内部推理当成唯一进度依据。
3. 恢复入口必须说明已完成、未完成和结果未知的动作，并且只能从平台提供的安全检查点继续。
4. 验收：不再出现“文字说了下一步但界面已经结束”、菜单还在却 run 已丢失或失败后只能整轮重来的状态冲突。

完成记录（2026-08-12）：

- 聊天消息不再只根据全局 `asking` 猜测 Agent 是否仍在执行；标准事件现在统一投影为准备、等待模型、准备工具、执行工具、等待确认、自动重试、完成、失败和取消状态，持久化收尾不会再冒充 Agent 执行中。
- 处理过程在没有工具步骤时也能显示真实运行阶段；工具记录、运行提示和结构化错误仍沿用现有轻量事件，不把模型内部 reasoning 作为唯一进度来源。
- 双写中断日志恢复时会把安全事件还原为合法 Workbench 事件，并将 run 检查点挂到当前恢复消息；安全检查点保留到续跑完成，避免恢复消息刚落盘就丢失续跑入口。
- 恢复卡片明确区分已完成工具、可安全继续、写入结果未知、等待确认失效和待执行工具；只有 `before_model` 或结果明确的 `after_tool` 能显示“从检查点继续”，其余状态禁止自动重放。
- 续跑复用原用户消息、原 assistant 消息、原 run 身份和 `NativeToolAgentLoop.resume()`，不重复追加提问、不重放已完成工具，也没有新增第二套恢复循环。
- 新增 `verify:agent-ui`，覆盖模型等待、自动重试、工具执行、终态投影、安全续跑和未知副作用拦截。

##### A8：主页与后台高级能力

1. 主页组件通过统一 `AgentContextProvider`、`AgentActionProvider` 和安全卡片模板接入，不在组件内部新增 Agent 循环。
2. 临时工作台只能使用本轮真实读取的数据和受控 HTML 协议；允许 AI 自由组合结构，但禁止脚本、样式、外链和任意前端代码执行。
3. 进化记忆、定时任务、周期任务和心跳任务共享同一 Profile、权限、Run Store、检查点与 eval。
4. 后台写操作不能绕过前台权限；低成本心跳只负责确定性检测，发现变化后再决定是否启动 Agent。
5. 验收：所有高级功能都能追溯到统一 run，能够单独授权、暂停、恢复和审计，没有新增平行执行框架。

A8 拆分顺序：

1. `A8.1` 主页 Agent 能力注册：把主页和组件真实可用的上下文、只读动作与写动作收口成平台能力快照。
2. `A8.2` 只读临时工作台：允许 AI 使用受控 HTML 标签、内置视觉类和思源跳转属性自由组合，由统一中心保存并供各 Agent 入口按需调用。
3. `A8.3` 工作台调整与固定：已按用户决策跳过，不在本次大版本开发。
4. `A8.4` 进化记忆：建立候选、整理、长期记忆、相关检索、冲突与遗忘生命周期及审阅面。
5. `A8.5` LLM Wiki：已从本次大版本取消，不建存储、向量索引或迁移层；未来需要时重新评估。
6. `A8.6` 后台 Job：统一定时任务、心跳任务、预算、权限、检查点、结果摘要和审计；A8.6.1 已完成，下一步为 A8.6.2。

A8.1 完成记录（2026-08-12）：

- 新增平台级 `AgentContextProvider` 与 `AgentActionProvider` 契约，统一声明主页作用域、组件作用域、数据敏感等级、真实工具/action、只读属性、幂等属性和确认要求。
- 复用原有 `homepage_manage`、组件业务工具和组件目录，建立唯一 `registerHomepageAgentCapabilities` 入口；删除运行组装层对主页管理与组件工具的分开注册，不新增工具或执行循环。
- 能力快照只从本轮 `ToolRegistry` 中实际存在且已被 Agent Profile 过滤后的工具生成；未授权、未启用或未注册的工具/action 不会出现在主页能力清单中。
- 每个可读取组件按照组件目录注册上下文来源；已有专用业务工具的组件复用业务工具，没有专用工具的展示组件只暴露经过裁剪的主页组件快照。
- 知识库、日记和数据库等已有工具被组件复用时仍指向原工具能力，不为主页复制一套读取实现；组件只登记自身需要的只读 action 白名单，不会把原工具的全部能力顺带暴露给主页智能表面。
- 当前阶段不生成卡片、不固定组件、不开放新写操作；A8.2 将直接消费能力快照实现只读声明式临时工作台。

A8.2 完成记录（2026-08-12）：

- 新增只读 `homepage_workbench` 工具并接入既有 Profile、ToolRegistry、ToolExecutor 和 observation log；Agent 必须先用现有工具读取真实数据，再把本轮结果排成临时工作台，没有新增第二套执行循环。
- 工作台不再受少量固定模板限制；AI 可以自由组合受控语义标签、响应式网格、统计、列表、徽标、状态和按钮等内置 `wb-*` 视觉类，能够生成不同信息密度与结构的工作台。
- HTML 在工具执行边界统一使用现有 DOMPurify 清理，并再次过滤类名和思源目标 ID；脚本、内联样式、外链、表单、iframe、事件处理器、未知类名和非法 ID 均不会进入渲染结果。
- 首期保持只读：只有按钮上的真实思源文档 ID 或块 ID 可以触发现有导航服务；修改、删除、写文档等业务动作不允许藏在 HTML 中，也不会绕过 AgentActionProvider 和确认流程。
- 临时工作台通过现有 AgentTurnResult 挂载到助手消息；工具边界把完整内容保存到统一仓库，消息只保留轻量引用，关闭再打开对话仍可恢复。保存和恢复时都会重新校验协议并清理 HTML，每条消息最多保留 3 个工作台；“临时”只表示不会固定到主页或写入业务数据。

A8.4 完成记录（2026-08-12）：

- 删除旧版“单一思源文档即全局记忆”的设置、读取、全文覆盖工具和 AI 知识库弹窗页；不读取、不迁移也不删除用户原有记忆文档，旧数据由用户自行决定是否保留或手工交给 AI 整理。
- 新版记忆中枢使用插件本地多文件 JSON：`profile.json` 只保存总开关和自动学习开关，`index.json` 保存轻量检索索引，每条正式记忆独立保存在 `items/{id}.json`，避免单文件持续膨胀和一次写入覆盖全部记忆。
- 每条记忆具备身份、偏好、目标、约束、项目、关系、决定或经历类型，并保存重要度、置信度、置顶状态、重复强化次数、来源入口和创建/更新时间；相同事实再次出现时强化原条目，不重复创建。
- 新增平台级聚合能力 `memory_manage`，统一提供 `search / remember / update / forget`；Agent 只能看到语义记忆和语义 ID，不知道文件路径及底层存储。开启“允许 AI 自动学习”时，Agent 可自主新增、修正和删除记忆；关闭时，所有记忆写入继续要求用户确认。
- 系统提示明确要求 Agent 主动但克制地学习：只保存用户明确表达、长期稳定且今后有用的事实；临时请求、工具结果、助手推断、第三方资料、密码、令牌和验证码禁止进入记忆。
- 每轮只按当前问题检索少量相关记忆，并始终优先注入置顶事实；当前用户消息与旧记忆冲突时以当前消息为准。记忆不再整份常驻上下文，降低长期对话中的 token 占用和信息干扰。
- 知识库对话拥有完整记忆读写能力；远程机器人复用同一存储、工具与全局自动学习策略；主页状态语只读取相关记忆；编辑器划词仍保持局部无状态，未创建第二套 Agent 或记忆实现。
- 在“主页设置 → AI 知识库 → 记忆中枢”新增统一审阅面，用户可总开关、关闭自动学习、搜索、按类型筛选、编辑、置顶和永久删除记忆；不能产生有效用户控制的底层参数不暴露。
- 旧版记忆设置字段不再进入 `KbSettings`，历史字段在下一次保存时自然丢弃；旧记忆文档本身完全不触碰。
- `homepage_workbench` 已加入统一工具帮助目录，模型可先查询正确参数；真实文档 ID 可直接生成跳转按钮，无需额外读取块 ID。同一轮并行产生的重复参数错误会先完整反馈给模型修正，不会在模型尚未看到错误时提前终止执行。
- 内置样式复用思源主题变量，包含自适应列数、窄屏单列、键盘焦点和最小点击面积；主题负责最终色彩，HTML 协议不允许模型自行注入视觉规则。
- 扩展 `verify:agent-platform` 覆盖类名白名单、思源 ID 校验、工作台 observation 提取与统一注册边界，并通过 TypeScript 检查及应用/Kernel 生产构建。

A8.2 工作台中心补全（2026-08-12）：

- 完整工作台从聊天会话中分离，统一保存到 `notebrain/workbenches/`；轻量索引记录标题、来源、生成时间、引用位置和空间占用，每个工作台内容单独存储，打开时再次执行协议校验与 HTML 清理。
- `homepage_workbench` 在通用工具执行边界直接登记工作台，因此知识库对话、主页自动任务和后续 Agent 入口只要按 Profile 注册该能力，就会共用同一仓库，不依赖知识库聊天组件。
- 聊天消息只保存工作台 ID、标题和生成时间，并显示紧凑的“打开工作台”入口；完整界面统一在思源弹窗中呈现，不再占据回答正文高度。
- 设置的“AI 知识库 → 临时工作台”提供统一列表，可查看来源、引用数量与空间占用，打开工作台，或在明确提示引用影响后删除；删除回答或会话时会同步解绑引用。
- 首次加载上一阶段保存过完整 HTML 的会话时，会把合法工作台迁入统一仓库并将消息收敛为轻量引用；新数据不再在聊天文件和工作台仓库中重复保存。

#### 提交与阶段交付规则

- 每完成一个阶段，只提交该阶段必要的文档、实现和验证，不把下一阶段的预备重构混入同一提交。
- 每阶段交付时给出一条十几至二十字左右的中文提交信息，格式采用“动词 + 核心对象 + 结果”，不使用模糊的“优化代码”“修复问题”。
- A1 建议提交信息：`建立Agent能力Profile与统一注册入口。`
- A2 建议提交信息：`统一Agent运行协议、模型用量与超时追踪。`
- A3 建议提交信息：`建立Agent最小权限装配与能力隔离。`
- A4 建议提交信息：`统一知识库Agent全能力运行入口。`
- A5 建议提交信息：`统一Agent上下文账本与安全恢复。`
- A6 建议提交信息：`统一机器人与轻量AI能力入口。`
- A7 建议提交信息：`统一Agent执行状态与安全恢复交互。`
- A8.1 建议提交信息：`建立主页Agent上下文与动作注册边界。`
- A8.2 建议提交信息：`建立受控HTML临时工作台与安全渲染边界。`
- A8.2 工作台中心建议提交信息：`建立临时工作台中心与跨入口调用机制。`
- 后续提交信息只描述已经验证完成的内容；阶段仍在开发中时不得提前标记完成。

## P0｜当前大版本主线

### 1. 设置发现与信息架构

#### 目标

在功能数量持续增长后，解决用户“知道有这个功能，但不知道在哪里设置”“无法判断设置影响哪个设备或主题”的问题。

#### 明确范围

- 为整个主页设置提供全局搜索。
- 搜索对象不是页面标题，而是具体设置项；结果应显示设置名称、简短说明、所属路径和作用域。
- 点击结果后直接打开对应设置页、滚动到目标项并短暂高亮，不能只停留在大分类入口。
- 为设置项声明作用域，例如：全部设备、当前设备、当前视图、当前主题、当前组件实例。
- 作用域必须来自设置项注册元数据，不能靠搜索页面重复维护一份映射表。
- 实时保存机制保持不变，不显示打断操作的全局消息。
- 设置页右上角可以在修改发生后短暂显示“保存中”“已保存”“保存失败/同步失败”；稳定后自动弱化或消失。
- 保存失败状态必须可点击查看原因或重试，不能只改变图标颜色。

### 2. Agent 执行体验

Agent 是本次大版本需要重点继续设计的方向。后续结合成熟 Agent 产品单独讨论并确定：

- 一轮执行的明确阶段，以及用户如何判断 Agent 仍在工作还是已经停止。
- 模型推理、工具调用、等待确认、继续生成、完成、取消、失败等状态的信息层级。
- 工具执行过程如何折叠展示，既不刷出大量过程文字，也不隐藏关键动作。
- 工具失败、模型不继续、超时、中断、配置不支持工具调用等错误的差异化反馈。
- 重试本轮、从失败步骤继续、切换模型、打开配置、复制诊断信息等恢复动作。
- 会话恢复、持久化和历史压缩状态如何被用户理解，而不是只存在于内部实现。

本节暂不确定最终 UI，后续应形成独立的 Agent 交互设计方案。

## AI Agent 平台专项（横跨 P0 与 P1）

这一专项不是新增一个聊天页面，而是把当前分散在 AI 对话、主页状态语、文档选中文字处理和机器人中的能力，收敛为一套可评测、可恢复、可扩展、可审计的 AI 平台。不同入口保持各自轻重和交互形态，但共用稳定的底层能力。

### 当前实现基础与主要缺口

经本地代码核对，当前 Agent 并非从零起步，已经具备以下基础：

- 桌面端对话和机器人已经复用 `NativeToolAgentLoop`，不是两套完全独立的 Agent 循环。
- 已有模型提供商适配、原生工具注册、权限确认、取消、超时、最大工具调用次数和完成/失败/取消状态。
- 会话可持久化，工具大结果会按类型压缩；当前上下文保留最近 10 轮原文，并维护阶段摘要、最近操作结果、目标文档/块索引和活动工作目标。
- 阶段摘要已有上下文压力判断：根据未总结轮次和上下文使用率产生 suggested、recommended、urgent、required 等状态。
- 已有执行中任务日志、最近 trace、模型 Agent 兼容性诊断、工具结果摘要以及部分面向用户的错误映射。
- 全局记忆写入已经具备并发摘要校验、写后回读和一致性验证，数据安全基础较好。
- 状态语和选中文字处理已走独立的轻量文本生成路径，说明项目已经具备“不同入口使用不同执行形态”的雏形。

当前最主要的问题集中在四个方面：

1. **上下文机制有实现，但缺少可证明的长期连续性。** 最近轮次、阶段摘要和操作记忆都有固定字符/条数上限，但尚未形成“哪些信息必须保留、哪些可按需取回、压缩后是否仍然正确”的可测协议。
2. **错误能被识别和展示，但恢复策略还没有统一。** 当前错误映射较丰富，机器人也有超时保护，但还缺少贯穿模型、工具和写操作的统一错误分类、重试条件、幂等判断与安全续跑检查点。
3. **全局记忆的存储安全强于语义能力。** 目前一条记忆基本等于文档中的一个段落，缺少作用域、类型、证据、置信度、冲突、过期、替代、遗忘和相关性检索，无法自然成长为个人 AI 助手。
4. **已有多种 AI 入口，但缺少统一能力契约和统一评测。** 各入口可以调用模型，却还不能用同一套标准证明它们在正确性、稳定性、成本、权限和记忆行为上符合预期。

### 平台目标架构

后续以一个共享的 Agent Kernel 为中心，每个入口只注册自己的 Profile，不再复制完整执行逻辑。当前“本地知识库 Agent 对话”是已有能力最完整、最适合作为抽取起点的参考实现，但不应让其他入口直接依赖它的页面、Svelte 状态或 Workbench UI。正确关系是：从它下面抽出通用内核和能力注册层，然后本地知识库对话也成为这套内核上的“全功能 Profile”。

这与主题系统的注册思想一致：主题通过统一契约注册 token、组件类别和展示变体；Agent 入口则通过统一契约注册模型调用、上下文、工具、记忆、MCP、技能、权限和执行策略。新增入口只进行装配，不重新编写一套对话、工具循环或错误恢复方法。

#### 分层能力栈

Agent 不是只有“普通对话”和“完整 Agent”两个档位，而应拆成可以逐层组合的能力：

1. **模型生成层**：统一单轮生成、流式生成、取消、超时、Provider 适配、错误归一化和用量记录。状态语、摘要、分类等轻任务只需要这一层。
2. **上下文对话层**：在模型生成之上增加多轮会话、上下文预算、阶段摘要和持久化。普通连续对话使用到这一层。
3. **工具 Agent 层**：增加 Agent 循环、工具选择、参数校验、权限确认、工具结果回传和安全恢复。入口可以只注册它真正需要的工具集合。
4. **知识与记忆层**：按需增加知识库检索、会话工作记忆、全局长期记忆、项目记忆和来源引用。长期记忆的读取和写入必须分别授权。
5. **扩展能力层**：按需增加 MCP、Skills/技能、网页能力、本地工作区能力和组件领域能力。每项扩展独立注册，不能因为启用了完整 Agent 就自动全部开放。
6. **自治执行层**：在上述能力上增加后台 Job、定时任务、心跳、检查点和恢复；这是权限最高的一层，只对明确场景开放。

因此，“本地知识库 Agent”不是一套要被其他功能整体复制的巨大实现，而是上面六层能力的完整组合；其他功能可以从最底层开始，按需求逐层增加。

#### 共享 Agent Kernel

统一提供：

- 模型与 Provider 能力协商。
- 工具注册、参数校验、权限确认和结果协议。
- 上下文预算与按需检索。
- 短期工作记忆、长期个人记忆和知识库检索。
- 执行检查点、取消、恢复与失败归因。
- trace、诊断、评测数据和成本/延迟统计。
- 后台 Job、定时任务和心跳任务的执行入口。

#### Agent Profile

每个 AI 入口注册以下元数据：

- `id` 与用途，例如 chat、selection、status、robot、homepage、background-job。
- 系统约束与输出契约。
- 可用上下文提供者和工具白名单。
- 长期记忆的读写策略。
- 是否流式、超时、最大步骤、最大成本和模型档位。
- 权限等级、远程调用限制和需要人工确认的动作。
- 失败后的允许恢复动作。

这样既共享稳定内核，也不会把状态语生成这种轻任务强行做成完整 Agent。

#### 能力注册与装配规则

为了避免后续再次出现“每个地方各写一套”，通用框架至少应提供以下可注册契约：

- `ModelGateway`：统一模型选择、Provider 适配、单轮/流式生成、取消、超时、错误和用量。
- `ConversationStore`：会话消息、压缩摘要、检查点和恢复数据的存取。
- `ContextProvider`：当前文档、选中文字、主页组件、机器人会话、知识库等上下文来源。
- `ToolProvider`：一组具有 schema、读写属性、风险等级、幂等性和权限要求的工具。
- `MemoryProvider`：会话记忆、项目记忆和全局记忆；分别声明读、候选写入和正式写入权限。
- `KnowledgeProvider`：全文检索、知识地图、向量/关键词检索和引用解析。
- `SkillProvider`：内置技能、用户技能及其提示模板和允许调用的能力。
- `McpProvider`：MCP Server 和工具索引；由 Profile 明确选择允许的 Server/工具，不能继承全部配置。
- `PermissionPolicy`：用户确认、远程调用、敏感数据、写操作和高风险动作规则。
- `ExecutionPolicy`：最大步骤、并行性、重试、超时、成本预算、模型档位和后台运行限制。
- `TraceSink`：所有入口使用同一 trace、correlation id、诊断和 eval 采样协议。

Profile 是这些能力的声明式装配结果，而不是一个包含大量 `if (isRobot)`、`if (isSelection)` 的中央函数。创建新入口时只声明“使用哪些能力、采用什么策略”，由统一 Composition Root 构造本轮运行环境。

能力必须遵守最小授权和显式依赖：

- 注册工具循环不等于开放全部工具；工具必须由白名单或能力包加入。
- 开放长期记忆读取不等于允许写入；候选记忆写入与正式晋升也要分离。
- 开放 MCP 不等于信任全部 MCP 工具；Server、工具和读写权限分别控制。
- Skills 只能引用当前 Profile 已开放的上下文和工具，不能通过技能绕过权限。
- 机器人、后台任务等非本地交互入口默认采用更窄的权限和数据作用域。
- Profile 和能力契约需要版本号；升级时由框架完成兼容检查，不能由各入口自行猜测字段。

#### 现有入口的复用现状与迁移原则

当前代码已经出现了正确方向的基础：

- 本地知识库对话通过共享 Provider 工厂、`AgentSession`、`NativeToolAgentLoop` 和每轮工具 Registry 组装完整执行环境。
- 机器人已经直接复用 `NativeToolAgentLoop` 与 Provider 工厂，再对允许工具做远程安全过滤。这证明“共享内核 + 入口 Profile”在现有代码中可行；后续应把它的工具、上下文、记忆、MCP 和 Skills 差异改为声明式 Profile，而不是继续手工补功能。
- 状态语和划词 AI 当前走轻量文本生成路径，这个形态本身正确；它们应接入统一 `ModelGateway`、错误、trace 和预算协议，而不是为了统一而强行进入多步工具循环。
- 本地知识库已有的全局记忆、MCP、Skills、知识检索等能力需要从 Workbench 装配中解耦为独立 Provider，之后其他入口才能按需选择，而不是复制实现或只能全部开启。

迁移过程中坚持“先抽象现有稳定实现，再迁移入口”，不一次性重写 Agent：

1. **已完成：**标记现有共享内核、WorkBench 专属逻辑和 UI 状态边界，并建立版本化 Profile 注册入口；本地知识库对话已先作为全能力 Profile 接管当前 Workbench 能力装配。
2. **已完成：**统一运行身份、事件、用量、Provider 错误、trace 和模型请求取消/超时协议。
3. **已完成：**复用工具 Registry、上下文 Builder、记忆、知识、MCP 和 Skills，在 Profile 中建立可组合能力与最小权限装配；暂不为单实现新增空壳 Provider 接口。
4. 将本地知识库对话剩余的模型、上下文、权限和恢复装配全部收敛到全功能 Profile，保证行为不变并以现有 eval 回归。
5. 把机器人改为远程受限 Profile，删除其可以由通用装配替代的平行代码。
6. 将状态语和划词接入轻量 Profile，只共享它们需要的层级。
7. 后续主页智能卡片、知识管家和后台 Job 只能通过这套注册入口接入。

### P0：对标成熟产品的 Agent 内核验证

对标重点不是模仿 Claude Code 或 Codex 的界面，而是验证它们所代表的成熟 Agent 方法：有限上下文下的高信号信息组织、工具结果驱动的连续执行、明确自治边界、可观察 trace，以及用代表性任务反复测量真实结果。

#### 上下文记忆管理

目标是保证 100 轮对话之后，Agent 不是机械携带全部原文，而是仍能正确处理关键事实、用户后续修正、未完成承诺和已经验证的工具结果。

上下文分为五个预算区：

1. **不可压缩区**：当前系统策略、用户明确约束、安全规则、当前任务目标和审批状态。
2. **近期原文区**：保留最近若干完整轮次；当前已有最近 10 轮基础，后续应根据模型窗口和任务压力动态调节。
3. **工作状态区**：活动目标、待完成步骤、最近可靠工具结果、写操作状态、用户最新修正和未解决问题。
4. **分层摘要区**：按任务阶段生成摘要，再将多个阶段摘要合并为更高层摘要；摘要必须记录覆盖范围和来源 turn id。
5. **按需检索区**：旧对话、原始工具输出和历史文档不常驻上下文，只保存可检索引用，需要时通过关键词、语义、时间和实体条件取回。

必须新增“上下文清单”诊断信息，至少记录每次请求注入了哪些类别、字符/token 占用、被排除的原因以及摘要覆盖到哪一轮。该信息默认不打扰用户，但可以在诊断面板查看。

100 轮连续性验收场景至少包括：

- 第 1 轮给出的稳定偏好，在第 100 轮仍可按需回忆。
- 第 20 轮事实被第 60 轮明确修正后，只采用最新有效版本。
- 第 30 轮开始但未完成的任务，在压缩后仍知道剩余步骤。
- 写工具已经成功、失败或结果未知时，不会在压缩后虚构成另一种状态。
- 被压缩的文档正文需要再次使用时，Agent 会重新读取，而不是把旧摘要当成原文。
- 长对话中不同项目/文档的上下文不会串线。

#### 工具执行异常与安全恢复

建立统一错误协议，不能只返回 `ok=false + message`。每个模型/工具失败至少包含：

- `category`：参数错误、权限拒绝、网络/限流/超时、Provider 协议、工具不存在或 schema 不匹配、业务冲突、数据版本过期、部分写入/结果未知、用户取消、循环超限、安全阻止。
- `retryable` 与可选 `retryAfter`。
- `safeToReplay`：是否可以安全重放。
- `sideEffectState`：`not_started`、`committed` 或 `unknown`。
- `userAction`：补充参数、授权、重试、切换模型、人工检查或无法自动恢复。
- `correlationId`：将 UI、trace、Provider 请求和工具执行串起来。

恢复规则：

- 仅对明确可重试且幂等的瞬时错误做有上限的指数退避；不能对所有失败无脑重试。
- 对写操作结果未知的情况先回读验证，禁止直接重放，以免重复创建或覆盖。
- 每次工具执行前后建立轻量检查点，保存已完成步骤、待执行动作和副作用状态。
- 工具失败后必须把结构化结果返回模型，使模型能改参数、换工具、降级或向用户询问。
- UI 必须区分“正在重试”“等待用户确认”“部分完成”“可从失败步骤继续”和“必须重新开始”。
- Provider 不支持原生工具、流式协议异常、模型输出中断等问题应归到 Provider 能力层，不让业务工具承担错误。

#### Agent 评测体系

发布前的“自己多用一段时间”仍然保留，但不能作为唯一验证方式。建立可重复运行的 Agent eval，并优先从历史真实故障、控制台日志和人工回归用例中整理第一批 20～50 个任务。

评测套件包括：

- 多轮上下文：回忆、修正、冲突、阶段摘要和 100 轮压缩后连续性。
- 工具选择：是否选择正确工具、参数是否完整、是否避免伪造工具调用。
- 故障恢复：超时、限流、断流、权限拒绝、无效参数、工具不存在、写后结果未知。
- 写操作真实性：最终数据库/文档状态是否正确、是否重复写、回复是否与真实结果一致。
- Provider 兼容性：不同模型的工具调用、继续生成、截断、空响应和多工具行为。
- 记忆：候选提取、去重、冲突替代、遗忘、相关性召回和错误记忆拦截。
- 知识库：检索来源、引用一致性、无答案时是否承认不足。
- 多入口：聊天、选中文字、状态语、机器人、主页卡片和后台 Job 的权限/上下文隔离。

每个任务保存输入、初始状态、允许工具、执行 trace、最终环境状态和评分结果。评分优先验证环境最终状态，而不是只判断 Agent 最后的自我陈述。

核心指标：任务成功率、回答完整度、错误事实率、工具失败恢复率、写操作一致性、多次运行稳定性 `pass^k`、延迟、token/成本、工具调用次数、长期记忆精确率/召回率和上下文修正命中率。确定性断言、模型评分和人工抽查组合使用，模型评分不能替代可程序化验证的结果。

### P1：可持续进化的个人 AI 记忆

#### 核心决策

不再把“指定文档中的若干段落”直接等同于完整记忆系统。现有文档可以继续作为用户可读、可编辑的记忆审阅界面，但长期记忆的权威状态应改为结构化数据；文档作为结构化记忆的投影或同步视图，不能继续兼任唯一数据库。

#### 记忆分层

1. **身份与稳定资料**：用户明确提供且长期稳定的信息。
2. **偏好、约束与目标**：写作偏好、工作方式、长期目标、明确禁忌和授权边界。
3. **项目/会话工作记忆**：当前项目状态、决定、未完成事项和临时假设，默认不永久保存。
4. **情景记忆**：完成过的任务、可靠工具结果和重要决策，可按时间和对象检索。
5. **语义记忆**：从知识库提取的实体、主题与关系，只保存索引和证据引用，不把整篇笔记复制进提示词。
6. **原始会话档案**：用于追溯和按需检索，不自动全部注入新会话。

#### 记忆记录模型

每条记忆至少包含：

- id、内容、类型和作用域（用户/项目/会话/入口）。
- 状态（候选、有效、已替代、已拒绝、已过期）。
- 来源 turn、文档或工具结果的证据引用。
- 置信度、重要性、创建时间、最后确认时间和可选有效期。
- 标签/实体、冲突对象、替代关系和是否被用户固定。
- 敏感等级与允许在哪些 Agent Profile 中使用。

#### 记忆生命周期

采用“两阶段”机制，而不是模型想到什么就直接写入长期记忆：

1. 从用户明确表达、重复行为、工具执行结果和任务 trace 中产生候选记忆。
2. 先写入会话级暂存区，记录证据和置信度。
3. 会话结束或后台整理时做去重、合并、冲突检测、稳定性判断和敏感信息过滤。
4. 高影响、敏感或推断性内容需要用户确认；低风险且证据充分的偏好可以自动晋升，但必须可撤销。
5. 晋升为长期记忆后，只在与当前任务相关时检索注入。
6. 长期未使用、已有新证据否定或具有有效期的内容进入过期/待确认状态，而不是永久积累。

严禁通过一次偶然表达就推断疾病、政治倾向、财务状况、亲密关系等敏感人格资料。AI 可以主动发现“可能值得记住”的内容，但用户必须能看到“为什么记住、来自哪里、在哪些地方使用”，并能编辑、删除、固定、禁止记忆或要求忘记。

#### 记忆体验

- 在设置或独立记忆中心展示候选、有效、冲突、待确认和已过期记忆。
- 对每条记忆提供来源、最近使用时间和“为什么本轮使用了它”。
- 对话中支持自然语言指令：记住、不要记住、仅本次使用、修改这条记忆、忘记与某主题有关的内容。
- Agent 可在合适时提示“我发现一个可能长期有用的偏好，是否保存”，但不能频繁打断。
- 允许导出、备份和彻底清理；删除后不能继续从旧摘要偷偷恢复。

### 未来候选：LLM Wiki / AI 知识管家（本次取消）

2026-08-12 决策：`LLM Wiki` 从本次大版本开发路线取消。当前不创建 Wiki 数据库、向量索引、Embedding 配置、后台索引 Job 或旧数据迁移层，避免在插件、Docker 和移动端运行边界尚未稳定时提前承担一套长期数据库。未来等插件稳定、思源内核插件与跨端数据能力成熟后，再根据当时产品和技术现状重新立项。下方内容仅作为未来需求备忘，不再属于当前实施计划。

#### 核心能力

- 建立主题、实体、人物、项目、概念和时间线的知识地图，并关联到原始文档/块证据。
- 为文档生成可更新的 AI 元数据：摘要、关键结论、实体、日期、任务、状态、来源和置信度。
- 发现可能的关联、缺失链接、重复内容、相互矛盾的结论、长期未更新内容和知识空白。
- 形成“知识整理收件箱”，将建议以来源、理由和预览差异呈现，由用户批量接受、拒绝或稍后处理。
- 支持从多个文档生成带引用的主题简报或动态 Wiki 页面；每个结论必须能回到原文证据。
- 定时或在知识库变化后增量刷新索引与摘要，不能每次全库重算。
- 将知识库中的任务、项目、人物和事件暴露给日历、强化日记、临时工作台与 Agent，但通过统一领域接口读取。

#### 数据与安全边界

- 确定性索引（文档、块、引用、标签、时间、数据库字段）与 AI 推断（主题、摘要、关系、冲突）分层保存。
- AI 生成内容必须带生成时间、模型版本、来源和置信度，不能冒充用户原文。
- 默认采用“建议 → 预览 → 应用”，不静默覆盖手写笔记；批量操作可撤销并保留变更记录。
- 删除、移动、大规模重写等高影响操作必须人工确认。
- 已有知识地图读取、全文检索和 Agent 工具应作为基础能力继续扩展，不再另建平行的全库扫描实现。

#### 首期落地范围

1. 文档 AI 元数据与增量索引。
2. 主题/实体知识地图和来源跳转。
3. 重复、冲突、缺链和陈旧内容建议。
4. 知识整理收件箱与可撤销应用。
5. 带引用的主题简报。

首期不做“AI 无需确认自动重构整个知识库”。

### P1：主页的全方位 Agent 接入

主页是信息聚合和行动入口，不应在每个组件里重复放一个聊天框。接入方式以“能力注册 + 安静的智能表面”为主：

- **今日简报**：根据日历、任务、习惯、强化日记、专注和知识库变化形成可追溯摘要。
- **智能工作台卡片**：Agent 根据当前目标临时生成可操作卡片，沿用已经确认的临时智能工作台方向。
- **建议收件箱**：集中显示知识整理、任务风险、日程冲突、遗忘事项和可执行建议，避免弹窗打扰。
- **组件上下文动作**：组件注册“询问此内容、总结、创建任务、安排日历、开始专注”等能力；通过右键或已有操作入口触发，不新增常驻悬浮按钮。
- **后台任务结果**：定时任务和心跳任务在主页展示摘要、异常和需要确认的动作，不展示冗长执行日志。
- **跨组件任务编排**：用户用自然语言提出目标，Agent 调用已注册领域能力完成操作，优先于要求用户维护大量手工规则。

每个组件如需被 Agent 使用，应通过统一注册入口声明：

- `AgentContextProvider`：可以向 Agent 暴露什么数据、数据作用域和敏感等级。
- `AgentActionProvider`：允许执行哪些动作、读写属性、幂等性和确认要求。
- `AgentWorkbenchHtmlProtocol`：允许使用哪些语义标签、内置视觉类、跳转属性和安全边界。

用户应能查看一次 Agent 行动使用了哪些主页数据，并在全局、设备、组件或单次任务层面关闭授权。主题只控制这些智能表面的视觉表现，不能改变权限和业务含义。

### 各 AI 入口的定位与边界

| 入口 | 执行形态 | 默认上下文 | 工具与记忆策略 |
| --- | --- | --- | --- |
| 本地知识与对话 | 完整 Agent | 当前会话、知识库检索、相关长期记忆 | 可使用完整授权工具；支持多步骤、确认、恢复和阶段摘要 |
| 自动状态语 | 单次轻量生成，不进入完整 Agent 循环 | 仅用户选择的真实统计事实与风格配置 | 只读、无长期记忆写入、缓存/防抖、失败回退到上次成功或普通状态语 |
| 文档选中文字 AI | 默认无状态的局部处理 | 选中文字、技能提示和必要文档元数据 | 默认无工具和长期记忆；“在对话中继续”时显式转入完整会话 |
| 机器人 Agent | 共享 Agent Kernel 的远程受限 Profile | 机器人会话、明确授权的知识与相关记忆 | 工具白名单更窄、超时更严格、写操作确认和远程身份审计；继续复用共享循环 |
| 主页智能表面 | 摘要、建议或临时工作台 | 用户授权的组件快照与当前主页作用域 | 通过组件能力注册调用；默认先建议，写操作按风险确认 |
| 定时/心跳任务 | 后台 Job Profile | 固定任务输入、上次检查点和最少必要记忆 | 有成本/次数/时间预算；产生可审计结果，不能无限自主循环 |

### Agent 平台实施顺序与验收门槛

1. 先把现有 trace、错误码、会话压缩和多入口调用整理成统一 Profile/错误协议，不先大改 UI。
2. 建立第一批真实失败 eval，覆盖“模型说下一步但停止”、工具失败、断流、写入未知、100 轮上下文和机器人超时。
3. 增加上下文清单、分层摘要和历史按需检索；用 eval 证明压缩前后结果没有明显退化。
4. 将全局记忆升级为候选 → 会话整理 → 长期记忆 → 相关检索 → 衰减/遗忘的生命周期，并保留用户审阅面。
5. 将聊天、状态语、选中文字和机器人正式注册为不同 Agent Profile，统一诊断与权限协议。
6. 实现主页 AgentContextProvider/AgentActionProvider 注册层，再接今日简报、建议收件箱和临时智能工作台。
7. 接入定时任务、心跳任务和更高自治能力；自治范围必须由评测结果和权限模型逐步放开。LLM Wiki 已从本次实施顺序移除。

每一阶段必须同时满足：构建/类型检查通过、关键 eval 无回归、真实环境最终状态正确、失败可解释、取消后不继续写入、诊断信息中不泄露密钥和不必要正文。

### 对标参考基线

- OpenAI 的 Agent/模型指导：多轮与工具调用应保留必要推理/输出状态，控制重复上下文，并为代表性任务测量成功率、完整度、延迟、token 和成本。
- OpenAI 的长期记忆示例：会话内先记录候选，再异步整理到结构化长期记忆；包含去重、冲突处理、遗忘和相关性注入。
- Anthropic 的上下文工程：有限上下文会出现信息稀释，应组合高信号常驻信息、压缩、结构化笔记和按需检索。
- Anthropic 的 Agent eval：以任务、trial、grader、trace 和最终环境结果构成评测，并关注多次运行稳定性而非单次成功。

参考链接：

- https://developers.openai.com/api/docs/guides/latest-model
- https://developers.openai.com/cookbook/examples/agents_sdk/context_personalization
- https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents
- https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents

### 3. 组件与主题的体验契约

#### 目标

主题不再针对大量具体组件逐个编写 CSS 特例，而是针对稳定的组件类别和展示变体提供统一表现。

#### 注册层次

1. **呈现类别**：只描述主题需要处理的视觉骨架，不表达任务、日历、媒体等业务功能。
2. **展示变体**：描述同一组件内部会改变布局骨架、外壳关系或内容密度的稳定模式。
3. **组件专属覆盖**：只用于确实无法通过类别和变体表达的少数组件，不作为常规分类方式。

第一版已经注册 9 个主题呈现类别：

| 呈现类别 | 主题适配含义 | 当前组件示例 |
| --- | --- | --- |
| `collection` | 重复行、条目、卡片或时间线组成的内容流 | 最近文档、收藏、任务、最近日记、热搜、条件文档、复习文档、纪念日 |
| `metrics` | 少量关键数字和短标签组成的信息摘要 | 天气、统计卡片 |
| `visualization` | 图表、热力图、数据画布或查询结果 | 热力图、SQL 查询、可视化图表、数据库图表 |
| `editorial` | 阅读、书写或长文本优先的内容面 | 自定义文字、自定义文档、便签、每日一句、星座运势 |
| `media` | 图片、封面或播放内容占视觉主导 | 图片轮播、音乐播放器 |
| `control` | 单一操作目标明确的计时器或交互工具 | 专注计时、倒计时、赛博木鱼 |
| `embedded` | 由外部页面或隔离运行时主导的内容 | 网页浏览器 |
| `workspace` | 多区域、多状态、内部导航并存的复合工作台 | 固定资产、增强日记、记账、AI 知识库 |
| `intrinsic` | 自带完整造型、不适合被主题强行重排的仿真/自成形内容 | 时间日期、黄历 |

业务侧原有的 `kind` 暂时保留用于兼容，但内置主题已经优先从 `categories` 注册入口解析。新主题不应再根据 `task / calendar / chart` 等业务名称推测视觉结构。

第一版已登记以下会实际改变视觉骨架的展示变体：

- 最近日记：列表、日历。
- 纪念日：列表、紧凑、卡片、时间线。
- 天气：默认、简约 1、简约 2。
- 自定义文档：标准、紧凑、沉浸、自定义。
- 时间日期：经典、简约 1、简约 2、仿真表盘。
- 可视化图表：进度图、标签云。
- 便签：纯色、纹理。
- 数据库图表：折线、柱状、饼图、散点。
- 历史上的今天：列表、图片。
- 黄历：经典、传统。

数据来源、排序、筛选、颜色和轮播动画等不会改变组件视觉骨架的选项，不登记为主题展示变体，避免主题契约被业务配置污染。

#### 契约范围

- 外壳策略、背景、边框、阴影、圆角和内边距。
- 标题区是否显示、标题高度、操作区和内容区关系。
- 内容溢出、滚动容器、最小尺寸和宽高变化。
- 空状态、加载状态、错误状态和无权限状态。
- 标准、紧凑、沉浸等展示变体。
- 主题 token 与组件内部语义 token 的映射。

类别和变体已经由现有 Widget Definition 与 Theme Manifest 注册入口统一声明。运行时会暴露 `data-widget-presentation-category` 与 `data-widget-presentation-variant`，主题解析优先级为“展示变体 → 组件专属覆盖 → 呈现类别 → 旧 kind 兼容 → 通用回退”，不能通过散落选择器猜测组件类型。

### 4. 全局性能优化

> 2026-08-12：已完成，包括启动关键路径分层、后台能力空闲启动、组件数据有界并发读取、真实加载进度、时钟共享定时源、隐藏页暂停、性能样本、ECharts/Swiper/Howler 按需构建、通用并发工具收敛和通知调度设置快照复用。详见 `docs/global-performance-optimization-2026-08-12.md`。

- 建立首屏加载、组件初始化、主题切换、拖拽、滚动和长时间运行的性能观测数据。
- 将 Agent、ECharts、富文本、音乐等重型模块按功能延迟加载。
- 优先挂载当前分区和首屏组件；不可见组件延迟初始化或暂停高频工作。
- 统一管理组件的定时器、事件监听、Observer 和后台轮询生命周期。
- 长列表采用分页或虚拟化；图表和动画在隐藏、离屏或系统减少动画时停止更新。
- 组件独立加载和失败隔离，禁止某个数据源或组件阻塞整个主页。
- 加载进度必须由真实初始化阶段驱动，不能用计时器或动画模拟进度。

## P1｜明确要做的核心能力

### 5. Agent 临时智能工作台

#### 产品定位

允许用户通过自然语言，让 Agent 基于插件内置模板、数据源和安全动作，临时生成一组可以直接操作的卡片。它不是一段聊天回答，而是一次性生成的任务工作台、分析工作台或信息工作台。

示例：

- “把本周未完成任务和相关文档整理成一个工作台。”
- “显示最近一个月没有推进的项目，并列出下一步建议。”
- “汇总今天的日记、任务、专注记录和习惯完成情况。”
- “把最近阅读的文献、批注和对应思源文档放在一起。”

#### 预期体验

1. Agent 理解意图并决定适合本轮结果的信息结构。
2. Agent 调用现有只读工具和数据服务获取真实数据。
3. Agent 生成受控 HTML；运行时只接受白名单标签、内置视觉类和声明式思源目标 ID，而不是执行任意 HTML、CSS 或脚本。
4. Agent 面板渲染真实卡片；首期只支持打开思源文档或块，后续业务动作继续调用已注册的安全动作。
5. 用户可以继续用自然语言调整筛选、排序、分组和呈现方式。
6. 工作台默认是临时的；可以主动固定到主页或保存为可复用的智能视图。

#### 安全和架构边界

- Agent 只能使用已注册数据源在本轮真实读取的事实，不能在工作台中补造数据。
- 工作台使用版本化 `AgentTemporaryWorkbench` 包装受控 HTML；允许的标签、类名、思源 ID 属性和长度均由运行时校验，模型不能定义脚本、CSS、URL 或事件。
- 受控 HTML 是呈现协议，不是任意前端代码执行能力；所有内容先清理再进入 `{@html}`。
- 写操作继续走现有权限确认、预览、重复写防护和工具结果反馈。
- 完整临时工作台由统一仓库保存，聊天、主页和后续 Agent 入口只持有轻量 ID 引用；加载时重新校验协议并清理 HTML。“临时”表示不固定到主页、不成为正式组件，而不是关闭对话后消失。
- 固定到主页后，应作为一种正式组件类别/展示变体参与主题适配。

#### 分阶段建议

- 第一阶段：只读结果、受控 HTML、内置视觉类、统一临时仓库与按需弹窗调用。
- 第二阶段：允许调整视图并固定为智能组件。
- 第三阶段：开放受控写动作和与定时任务联动。

### 6. 强化日记工作台作为底层承载

#### 核心决策

晨间计划、晚间复盘、周期回顾等完整操作流程继续由“强化日记工作台”承载；主页不复制一套完整操作界面，只展示摘要、进度、提醒和进入对应操作的入口。

#### 主页应承担的内容

- 今日计划完成度、最重要事项和待处理项目。
- 昨日遗留、今日新任务、快速记录和待复盘数量。
- 当前日/周/月/年复盘状态。
- 晨间计划或晚间复盘尚未完成时的轻量提示。
- 点击具体摘要后，打开强化日记工作台并定位到指定日期、模块或操作步骤。

#### 实现边界

- 抽取无界面的强化日记领域服务和摘要 ViewModel，供主页组件、完整工作台和 Agent 共同使用。
- 主页不能通过隐藏挂载完整工作台来读取状态，也不能依赖工作台页面已经打开。
- 完整编辑、任务迁移、日记结构调整和复杂图表仍留在工作台。
- 主页摘要组件与工作台必须读写同一数据服务，不能维护第二份状态。
- 后续 Agent 临时工作台也可使用这些摘要和动作，但仍遵守工作台业务规则。

### 7. Agent 定时任务、心跳任务与后台执行

#### 产品方向

插件长期方向是 all-in-one AI 智能工作台。自动化不以用户手工搭建大量“触发器—条件—动作”规则为核心，而是让用户用自然语言要求 Agent 创建、修改、暂停和执行后台任务。

后台 Job 不是第二套 Agent，也不是把 `setInterval` 包一层 UI。它是统一 Agent 平台上的自治执行层：调度器只决定“何时检查”，确定性检测器决定“是否有变化”，只有真正需要理解、整理或行动时才调用 Agent。

#### 成熟产品与运行时对标结论（2026-08-12）

- ChatGPT 把一次性任务、周期任务和“有意义变化才通知”的监测任务收入独立 Scheduled 管理页，同时限制活跃任务数和最高执行频率。对本项目的启示是：任务必须可统一管理、可暂停，并且产品层必须有显式额度和频率上限。
- Gemini 和 Microsoft 365 Copilot 都允许从普通对话/提示直接创建定时动作，但仍在独立管理页编辑、暂停、立即运行和删除，使用外部应用时继续受应用授权约束。Copilot 把定时结果保存在 Chat 中，邮件只是“结果已就绪”的可选通知。因此“对话创建 + 中心管理 + 结果与通知分离”比把 Job 绑在某条聊天上更适合本插件。
- OpenClaw 明确分开精确 cron、隔离 Agent 运行与 `announce / webhook / none` 投递模式；Job 执行和结果送到哪里不是同一个配置。本项目应借鉴这种“一个调度器、不同执行语义、独立活动账本、显式投递”，但不借鉴其可直接执行任意命令的高权限默认。
- Home Assistant 使用“触发 → 条件 → 动作”，通知只是动作可选择的目标之一。对本项目的直接启示是：通知中心应继续负责渠道和投递，不能反过来承担 Agent 调度、数据监测和任务执行。
- LangGraph 把长时 Agent 的可恢复性建立在持久化检查点和稳定 thread ID 上，并明确要求中断前的副作用可幂等；Temporal Schedule 则把 catch-up 和 overlap 作为一等策略。本项目已经有检查点、副作用状态和安全重放协议，应直接复用，不引入这些框架。

对标资料：

- https://help.openai.com/en/articles/10291617-tasks-in-chatgpt
- https://support.google.com/gemini/answer/16316416
- https://support.microsoft.com/en-US/Microsoft-365-Copilot/schedule-your-most-used-copilot-prompts
- https://docs.openclaw.ai/cron
- https://docs.openclaw.ai/gateway/heartbeat
- https://docs.openclaw.ai/automation/tasks
- https://docs.langchain.com/oss/python/langgraph/persistence
- https://docs.langchain.com/oss/python/langgraph/interrupts
- https://api-docs.temporal.io/
- https://www.home-assistant.io/docs/automation/trigger/
- https://www.home-assistant.io/docs/automation/action

#### 现有项目审核结论（2026-08-12）

项目已经有很多可复用的底层，不需要引入 Temporal、LangGraph、n8n 或新的数据库：

- 任务通知、倒计时通知、强化日记通知和复习通知已实现补发窗口、运行中防重入、`occurrenceKey` 去重、投递历史和启停清理，这些应该成为统一调度的基础。
- 上述四个通知功能目前各自维护几乎相同的 `setInterval + shouldRun + scanOnce + reconcile`；通知中心另有移动端五分钟轮询。这些是首批应收敛到共享 Scheduler 的真实重复点，但必须逐个迁移并保持现有行为。
- 插件启动已把非关键运行时安排到空闲任务，卸载时也会销毁调度器；统一 Scheduler 应继续接入这个唯一生命周期入口。
- Agent 已有标准 `session/run/step/event`、Provider 错误分类、可重放性、副作用状态、用量、检查点和安全续跑；后台 Job 必须直接复用 `runAgentProfile`、`NativeToolAgentLoop` 与同一事件协议。
- 当前 Agent trace 只保留内存中最近少量运行，中断日志只面向当前会话恢复；后台 Job 需要独立、有界的持久化 Run Ledger，不能把结果混进普通聊天历史。
- 现有共享 JSON 存储已有串行写、写后回读和版本/修订校验；记忆中枢也已验证“索引 + 分片 JSON”结构。Job Store 应复用这些边界，不新增 SQLite 或第三方存储层。
- `navigator.locks` 和进程内写队列只能防当前运行时的并发，不能作为多设备分布式锁。现有机器人的“指定运行设备”比自制云端选主更适合首期 Job Runner。
- 通知中心已经具备通用 `NotificationEvent`，其 `source/type/content/occurrenceKey/url/extra` 足以承载后台任务结果；已经支持桌面、移动、默认外联和指定外联目标，Webhook/飞书渠道还具备限流、去重、逐目标投递历史和失败隔离。后台 Job 不需要新建第二套消息系统。
- `NotificationTargetSelector` 已被任务、倒计时、日记、专注和复习设置共同复用，自动化中心应直接复用它选择投递方式。现有移动计划 Provider 也能提前把固定提醒登记到原生移动端，但动态摘要只有在某个 Runner 实际运行后才能生成，不能假装可以被提前登记。
- 机器人平台已经有统一 `RobotOutboundMessage`、Provider Manager 和主动 `send` 契约；微信运行在 Kernel，飞书/QQ 运行在 Electron，且已有唯一 `runtimeOwner`。新增“机器人通知”应复用这条出站链路，而不是把后台结果伪装成一条入站用户消息重新走一遍对话 Agent。

#### 统一运行模型

```text
自然语言 / 设置页 / 内部业务注册
                    ↓
          版本化 Job Definition
                    ↓
        Scheduler（只计算到期与补发）
                    ↓
       Sensor（低成本、确定性、可去重）
             ↓ 只有命中才进入
       Background Agent Profile / 直接动作
                    ↓
     Run Ledger / 结果正文
                    ↓
      Delivery Bridge → 通知中心 → 目标渠道
```

标准 Job Definition 至少包含：

- 身份：`jobId`、名称、来源入口、创建时间、版本和启用状态。
- 触发：一次性时间、日/周/月周期或固定间隔，并显式保存 IANA 时区。首期不暴露原始 cron 表达式，常用调度足够覆盖产品需求。
- 任务：任务种类、固定目标、Profile ID、允许的上下文/工具/action、数据作用域和结果投递方式。只保存声明式数据，不保存任意 JS、HTML 或命令。
- 策略：补发方式、重叠方式、超时、重试上限、连续失败自动暂停、安静时段、每日次数和 token/工具/时长预算。
- 执行权：指定的运行设备 ID 与该设备当前能力。任务可在任意设备管理，但只由用户明确指定的 Runner 执行。

Job Definition、可变运行状态和运行历史分开保存，避免修改调度时覆盖执行进度：

- `automation/jobs/{jobId}.json`：用户可编辑的声明式任务。
- `automation/state/{jobId}.json`：下次执行、上次指纹、连续失败、暂停原因和 Runner 状态。
- `automation/runs/{yyyy-mm}/{runId}.json`：有界保留的执行记录，保存标准 run 身份、耗时、模型用量、工具摘要、结果、错误和安全检查点；到期清理时不影响 Job Definition。

#### 调度、心跳与事件的边界

#### 任务类型

- **一次性定时任务**：在指定时间提醒、查询、整理或执行获批动作。
- **周期任务**：按日、周、月或固定间隔执行固定目标；每次默认使用隔离上下文，不向普通聊天历史追加一轮消息。
- **心跳任务**：按可控频率运行已注册 Sensor，比较结构化快照与上次指纹；没有变化时只更新检查时间，不调模型、不生成聊天和通知。
- **事件任务**：后续可监听插件内部事件，如任务完成、日记创建、专注结束或数据同步完成。

“心跳”和“周期 Agent 任务”不是同义词。只有“检查本周项目是否出现新风险，有变化才总结”适合心跳；“每天 22:00 生成今日复盘”应是精确周期 Job。所有心跳共用 Scheduler，但各业务只注册 Sensor，不再自己建立轮询器。

#### 三种实际执行方式

1. **固定提醒**：例如“明天 9 点提醒我交材料”。Agent 只在创建时把自然语言编译为结构化 Job；到点后 Scheduler 直接生成通知事件，不再调用模型。这样最省成本，也不会因为模型暂时失败而丢掉闹钟。
2. **定时 Agent 任务**：例如“每天 8 点整理热搜、今日待办和日程后发给我”。到点后由 Scheduler 直接调用 `background-job` Profile，Agent 使用获准工具完成一轮隔离运行，结果先写入 Run Ledger，再由通知中心投递摘要或机器人正文。不是把任务文本发给聊天 AI 等它重新理解。
3. **变化监测任务**：例如“监控某页面，变化后总结影响并通知我”。Scheduler 先运行无模型 Sensor，用 ETag、更新时间、版本号、数量或规范化内容哈希与上次 checkpoint 对比。无变化只更新检查时间；有变化才按 Job 定义直接通知、调用 Agent 解释，或执行已获准的低风险动作。

监测任务的幂等键使用 `jobId + changeFingerprint`，固定/周期任务使用 `jobId + scheduledSlot`。因此同一次变化或同一时间槽即使被多次唤醒，也只能产生一次正式运行和一次针对每个目标的有效投递。

#### 与通知中心的融合决策

自动化中心和通知中心保留为两个产品面，但只通过一条共享投递协议连接：

- **自动化中心负责**：为什么运行、何时运行、检查什么、是否调用 Agent、允许哪些工具、预算、Runner、执行状态和完整结果。
- **通知中心负责**：结果发到哪里、各渠道格式、安静时段、限流、去重、重试、逐目标状态和投递历史。
- **Run Ledger 是完整事实来源**：保存完整摘要、生成内容、工具结果和错误；通知事件只保存适合渠道展示的标题、短摘要和打开该 run 的链接，不复制大段正文或临时工作台 HTML。
- **通知中心不改造成 Scheduler**：固定任务、Agent 任务和 Sensor 都不能塞进通知渠道实现；通知中心只接收已经产生的 `NotificationEvent`。
- **不需要整体改名**：用户界面继续使用容易理解的“通知中心”，可把内部小标题升级为“通知与渠道”，增加“自动化”来源筛选和机器人目标；不再另建“Agent 消息中心”。
- **投递契约必须与运行时无关**：共享层只定义 `DeliveryEnvelope = event + targets + runId`，不能依赖 `window`、Svelte 或 Electron。前端 Adapter 继续调用现有通知中心完成桌面、原生移动和当前外联投递；Kernel Adapter 只处理 Kernel-safe 的微信/HTTP 外联，并把当前不可达的本地目标保留为待投递状态，等对应前端恢复后对账。不能为了后台 Job 复制通知规则、历史和去重实现。

后台运行完成时统一产生：

```text
NotificationEvent {
  source: "automation",
  type: "reminder | job_completed | job_failed | approval_required | change_detected",
  occurrenceKey,
  title,
  content,
  url: 打开对应运行详情,
  extra: { jobId, runId, resultKind, artifactId? }
}
```

一条逻辑事件可以扇出到多个目标，各目标独立记录成功、失败和重试。Job 成功与通知成功是两个状态：任务可以成功但某个渠道投递失败，不能因为通知失败就重跑整个 Agent。

#### 机器人通知与主动消息

- 在通知中心新增的是**机器人投递目标**，不是第二个机器人 Agent。目标只引用已授权的 `provider/account/chat` 路由，不在 Job 中复制密钥。
- 用户从机器人聊天创建任务时，`automation_manage` 默认记录该会话的安全 `routeRef`；从本地知识库聊天或设置页创建时，由用户选择桌面、移动、外联渠道或已保存的机器人会话。
- 固定提醒到点后直接通过机器人 Provider 出站；Agent 任务则先由后台 Profile 执行，完成后再把最终结果通过同一 Provider 出站。两者都不能作为“机器人收到的新用户消息”重新进入 Robot Core，避免自身消息触发自身和形成循环。
- 机器人正文可比系统通知更完整；桌面/移动系统通知只展示标题和短摘要，点击后打开运行详情。消息过长时沿用 Provider 的长度限制与分段发送规则。
- 只有当前 `runtimeOwner` 和活跃 Provider 具备对应路由时才能发送。会话 ID、上下文 token 或平台权限失效时记录明确的 `route_unavailable`，尝试其他已选择渠道，但不能悄悄改投未授权会话。
- 当前机器人一次只启用一个 `activeProvider`；首期“机器人通知”只允许选择当前活跃 Provider 的来源会话或用户保存的默认会话，不虚构同时在线的多机器人。微信已有 Kernel 主动发送能力；飞书/QQ 目前依赖 Electron，桌面未运行时不保证投递。未来若确有 24 小时需求，只把它们的出站 Transport 下沉为 Kernel-safe Adapter，不把整套移动端或前端机器人服务搬进 Kernel。
- 首期不允许同一 Job 把结果重新投回创建对话的普通聊天历史；“关联对话”只用于展示任务入口和运行链接，避免后台轮次污染上下文压缩与重新生成语义。

#### 挂起、补发和重复执行

- JavaScript 计时器只是“下次唤醒闹钟”，不是时间事实来源。每次启动、恢复可见或时钟变化后，都从持久化计划重新计算 `nextRunAt`。
- 每个调度槽位使用 `jobId + scheduledSlot` 作为幂等键，同一次到期即使因重启、可见性变化或设置广播被重复扫描，也只能产生一个 run。
- 补发策略只允许“跳过”、“立即补一次”或“只补最新一次”；不因电脑关闭七天而爆发回放七天的 Agent 任务。
- 重叠策略默认为“上一轮未结束则跳过并记录”；可选“仅排队最新一轮”。首期不允许同一 Job 并行执行。
- 自动重试只适用于确定性读取、模型请求尚未产生副作用，或已由现有协议明确标注 `safeToReplay=true`的步骤。写结果未知时禁止整轮重放。
- 连续失败达到上限后自动暂停 Job，通知用户并保留最后一次可安全诊断的错误。

#### 跨端执行边界

- 桌面前端关闭或 WebView 被挂起后，普通插件计时器不可靠；不对用户声称“所有设备关闭后仍可执行”。
- 首期让用户指定一个自动化 Runner，优先选长时运行的桌面端或 Docker/Kernel 端；复用现有设备身份和机器人运行所有者的交互模式，不自制不可验证的多设备租约算法。
- 移动端可以管理 Job、查看运行结果，并继续复用现有本地通知计划实现精确提醒；在操作系统未授予稳定后台执行能力时，不承诺在 App 被挂起后运行完整 Agent。
- Docker 只有在插件 Kernel 运行时已加载、模型和工具能力可用时才能作为 Runner；网页被关闭不能单独支撑后台 Agent。

具体能力矩阵：

| 运行位置 | 可以可靠承担 | 不应承诺 |
| --- | --- | --- |
| 桌面端前端/Electron | 打开期间的前端安全工具、桌面通知、飞书/QQ 出站 | 应用完全退出后继续执行 |
| 思源 Kernel / Docker Runner | 24 小时 Scheduler、Kernel-safe Sensor/Agent 工具、微信与外联出站 | 调用只存在于浏览器 DOM 或桌面 Electron 的工具 |
| 原生移动端 | 创建和管理 Job、查看结果、提前登记内容已知的固定系统提醒 | App 被系统挂起后持续跑 Agent、轮询网页或维护机器人长连接 |
| 普通浏览器 | 页面打开期间管理和执行前端安全任务 | 关闭标签页后继续计时和运行 |
| 外部机器人客户端 | 接收 Runner 主动发送的结果 | 代替本插件执行 Job 或保存任务事实 |

多端统一采用“共享 Job、指定 Runner、多个投递目标”：

- Job 和 Run Ledger 可跨设备读取，但首期一个 Job 只指定一个 Runner，不实现容易重复执行的自动抢占或云端选主。
- 固定提醒可在移动端对账时提前登记本地系统通知；动态 Agent 结果和变化监测结果在生成前内容未知，必须由在线桌面/Kernel Runner 通过机器人、飞书、Webhook 等远程渠道推送，或等移动端下次打开后看到运行结果。
- 如果用户所有 Runner 都离线，又没有云端服务，则动态任务不可能准时运行。恢复后按补发策略只运行最新一次或跳过，并明确显示“执行设备离线”，不能伪装成跨端后台能力。
- 机器人助手继续不在移动端常驻。用户手机收到机器人主动消息，实际是指定的桌面/Kernel Runner 执行后通过微信、飞书或 QQ 服务发送，与移动端插件是否在后台无关。

#### Agent 执行、上下文与权限

- 新增一个受限 `background-job` Profile，仍调用统一 Agent Kernel。它默认不携带普通对话历史，`sessionId` 固定为 Job 作用域，每次触发生成新 `runId`，只注入 Job 目标、上次结构化快照、必要长期记忆和本轮真实数据。
- 调度时保存的工具和 action 白名单必须与执行时 Profile、用户当前设置、Runner 环境能力再次求交集。旧 Job 不能因新版本新增工具而自动获得更大权限。
- 创建一个持续后台 Job 等于授予未来执行权，无论由设置页还是对话创建，都必须在首次启用前展示调度、作用域、工具、预算和投递方式并由用户确认。
- 首期后台 Agent 只自动执行只读工具和已明确允许自动的低风险幂等动作。需要确认的写操作不在无 UI 环境中等待过期对话；它们生成持久化待审批卡片，由用户回到任务中心处理。
- 心跳与其他无人值守任务默认可读取相关长期记忆，但不得把网页、MCP、机器人或其他外部内容静默写入记忆中枢，避免后台内容污染持久影响后续 Agent。

#### 体验要求

- 用户可以直接说：“每天晚上十点检查今天是否完成复盘，没有就提醒我。”
- Agent 通过唯一 `automation_manage` 能力将自然语言编译为可校验 Job Definition，创建前展示时间、目标、Runner、数据权限、预算和可能执行的写操作。对话是创建入口，设置中的自动化中心才是唯一管理事实来源。
- 在“主页设置 → AI 知识库 → 自动化中心”提供概览、定时任务、心跳监测、运行历史和执行设备；查看下次执行、上次结果、运行耗时、失败原因、消耗和待审批动作。
- 每个 Job 复用现有通知目标选择器配置“完成后通知”，并可选“仅失败/需要处理时提醒”“有变化时提醒”“每次运行都提醒”；固定提醒不显示 Agent 模型和 token 预算设置。
- 支持暂停、立即运行、修改、复制和删除。
- 任务运行后只在有结果、失败或需要确认时提醒，避免心跳任务产生通知噪音。
- 后台运行结果进入自动化活动账本和通知中心，不在本地知识库对话中自动追加助手消息。

#### 安全与运行要求

- 后台任务必须保存为声明式 Job，不保存无法审查的任意执行代码。
- 每次运行具备唯一 ID、幂等保护、超时、重试上限、连续失败暂停和完整运行记录。
- 写操作必须遵守现有 Agent 权限策略；后台环境不能绕过需要确认的操作。
- 支持安静时段、并发限制、模型/额度限制和无网络恢复。
- 心跳先执行低成本确定性检查，只有命中条件才调用模型，避免持续消耗。

#### 分阶段实施顺序

1. `A8.6.1｜Job 契约与存储`：已完成。建立 Job/State/Run 三层协议、严格校验、分片 JSON、写后回读和保留策略；契约区分固定提醒、定时 Agent 和变化监测，此阶段不执行 Agent。
2. `A8.6.2｜统一 Scheduler`：实现到期计算、时区、挂起恢复、补发、重叠、幂等键、暂停与立即运行；先用确定性 demo Job 验证。
3. `A8.6.3｜Runner、运行账本与投递桥`：接入指定设备、状态机、取消、超时、持久化结果和连续失败暂停；把自动化事件接入现有通知中心，并增加复用 Robot Provider 的机器人投递目标。先让固定提醒完整跑通，不依赖模型。
4. `A8.6.4｜后台 Agent Profile`：复用统一 Agent Kernel、现有错误/检查点协议和工具授权；首期只开放只读与安全低风险动作。
5. `A8.6.5｜心跳 Sensor 注册`：先接任务过期、强化日记状态、通知失败等真实数据；用指纹证明未变化时不调模型。
6. `A8.6.6｜自动化中心与自然语言管理`：建立统一管理页、待审批面和 `automation_manage`；手工结构化创建稳定后再开放 Agent 创建。
7. `A8.6.7｜旧调度器收敛`：为四类旧通知先固定现有行为校验，然后逐个迁入 Scheduler 注册层；每迁移一类就删除它的私有轮询器，不一次性重写通知业务。

#### A8.6.1 完成记录（2026-08-12）

- 建立严格的 Job Definition、Job State 与 Run Record 契约，明确一次、每日、每周、每月、间隔和低成本 Sensor 触发方式，并在契约层阻止任务类型与触发器错配。
- Job Definition 统一保存来源、执行设备、能力要求、补发/重叠/失败策略和投递目标；投递支持桌面、移动端、现有外联与机器人路由引用，不复制通知中心实现。
- 数据按 `jobs / state / runs/<month>` 分片保存；所有读取严格校验，损坏数据会停止覆盖，所有写入和删除均回读验证，并用 revision 阻止多窗口静默覆盖。
- Run Record 固定运行身份和终态，保存幂等 occurrenceKey、Runner、用量、工具摘要、结果、错误与安全检查点；按月索引，最多保留 12 个月、每月 200 条。
- 新增最小可运行校验，覆盖新建、并发 revision 冲突、状态绑定、运行终态不可重开、单条运行恢复定位和删除校验。当前没有启动 Scheduler、Agent 或通知投递，不改变任何已有通知功能。

#### 必须通过的专项验收

- 应用重启、页面长时挂起、跨日、时区切换和夏令时后，下次执行可重算且不重复。
- Runner 离线后按 Job 策略跳过或只补最新一次，不产生爆发任务。
- 同一调度槽位被重复唤醒、设置重新加载或多窗口打开时只产生一个 run。
- 上一轮运行未完成时不并发重复执行；取消后不继续调用模型或写入业务数据。
- 心跳连续无变化时 Provider 请求数为 0；有变化时只产生一条有意义的结果或通知。
- Provider 限流、断网、可重试错误和写结果未知分别走不同策略；任何未知副作用不得自动重放。
- 后台 Job 不能获得 Profile 未授权工具，不能在无人值守时绕过确认，不能把外部内容静默写入长期记忆。
- 运行历史必须有界清理，记录中不保存 API Key、完整文档正文、本地绝对路径或不必要的模型推理。

### 8. 高度自定义的全局日历组件

#### 产品定位

主页缺少一个跨业务模块的统一日历。该组件不是强化日记工作台日历的复制品，而是所有时间型信息的聚合视图和操作入口。

#### 数据源

- 强化日记：日记存在状态、计划、复盘、快速记录。
- 任务和任务管理 Plus：开始、截止、完成和延期信息。
- 习惯打卡：计划打卡、完成、补签和中断。
- 番茄钟：专注时段和累计时长。
- 纪念日、倒计时、提醒及其他后续注册的时间数据。
- 外部文献阅读、记账等模块可选择性提供时间事件，但默认不应让日历过载。

#### 统一事件接口

各业务模块通过 `CalendarEventProvider` 一类注册器提供事件，不让日历组件直接了解所有业务文件。事件应包含来源、类型、标题、时间范围、颜色语义、只读/可写能力、详情动作和跳转目标。

#### 展示与自定义

- 月视图、周视图、日程列表为基础视图；后续可增加年度热力视图。
- 用户可以选择数据源、事件类型、颜色、图标、显示字段、每格最大数量和溢出策略。
- 支持按项目、标签或业务类型建立过滤视图，并保存为组件配置。
- 不同主题通过“日历类别 + 月/周/列表变体”统一适配。
- 小尺寸组件优先显示摘要点、数量和主要事件；放大后逐步展开详情，不能把完整月历硬塞进狭小卡片。

#### 操作边界

- 点击事件可查看详情并跳转到对应工作台或来源文档。
- 支持能力由数据源声明；只读事件不能因为日历支持拖拽就被错误修改。
- 拖动改期、快速新增和跨日调整属于后续阶段，第一阶段先保证聚合显示、筛选和可靠跳转。

### 9. 习惯打卡系统与主页组件

#### 产品定位

习惯不是无限重复的普通任务。它需要独立的数据模型、计划规则、每日完成记录、连续性和趋势分析，并能够与日记、日历、番茄钟和 Agent 联动。

#### 习惯模型

- 名称、说明、图标、颜色和归档状态。
- 计划：每天、每周指定日期、每月次数、周期次数或自定义间隔。
- 打卡类型：完成/未完成、次数、时长、数值或等级。
- 目标值、最低有效值、允许补签、跳过规则和提醒时间。
- 可选关联项目、标签、日记字段或专注目标。

#### 打卡记录

- 每条记录保存习惯 ID、日期、结果、数值、备注、来源和修改时间。
- 区分完成、部分完成、跳过和未完成，避免“合理休息”被算作失败。
- 补签必须被标记，统计时可选择是否计入连续天数。

#### 主页体验

- 今日打卡：适合小卡片快速完成。
- 周期进度：显示本周目标、已完成次数和剩余次数。
- 连续记录：当前连续、最长连续和最近中断原因。
- 点击后进入习惯中心查看月历、趋势、记录和设置。

#### 联动

- 全局日历显示习惯计划和完成状态。
- 番茄钟可以绑定“阅读 25 分钟”等时长型习惯，专注结束后自动形成候选打卡。
- 强化日记可以插入今日习惯摘要和复盘问题。
- Agent 可以查询趋势、提醒异常和辅助制定习惯，但不能擅自补签或修改历史。

### 10. 高级番茄钟与专注数据中心

#### 现有基础

当前已经保存专注会话 ID、开始/结束时间、本地日期、计划时长、实际专注时长和完成/取消状态，并具有年度明细与汇总索引。升级应扩展现有专注数据体系，而不是重新建立一套计时记录。

#### 任务、项目和习惯绑定

- 开始专注前可以选择无绑定、任务、项目或习惯。
- 从任务或习惯入口启动时自动带入绑定对象。
- 会话记录增加稳定对象引用、对象类型和必要的标题快照；统计以稳定 ID 为准，标题快照只用于对象删除后的历史展示。
- 完成专注后，把实际时长计入对应任务、项目或时长型习惯。
- 任务详情能够查看累计专注时长、专注次数和最近一次专注。

#### 番茄钟能力升级

- 自定义专注、短休息、长休息和循环规则。
- 暂停、提前完成、取消、跳过休息及中断原因记录。
- 可选的专注目标、当前任务摘要和下一轮提示。
- 通知、声音、全屏/沉浸显示和迷你状态应继续遵守系统动画、音量和通知设置。

#### 专注数据中心

- 今日、周、月、年专注时长和完成次数。
- 按任务、项目、习惯、标签和时间段分析。
- 日历热力图、时段分布、专注时长趋势和完成率。
- 计划时长与实际时长偏差、取消原因和中断分布。
- 最近会话明细，可跳转到绑定对象。
- 数据中心是完整分析入口；主页番茄钟组件只保留当前计时、今日摘要和快速开始。

## P2｜明确保留、逐步推进

### 11. 数据库数据转图表组件

#### 当前状态

项目已经存在 `databaseChart` 渲染器、组件注册、ECharts 实现和部分配置字段；桌面端内容设置中的类型入口和设置组件被注释隐藏。后续不是简单取消注释，而是以当前数据库结构、主题契约和移动端配置方式重新整理后再开放。

#### 目标能力

- 选择一个思源数据库/属性视图作为数据源。
- 选择分类、数值、日期等字段，并对字段类型进行明确校验。
- 支持直接数值、计数、求和、平均、最大/最小和按时间分组。
- 支持折线、条形、饼图，以及后续适合日期和分布数据的图表。
- 支持排序、筛选、空值处理、Top N、图例、标签和提示框配置。
- 图表点击后可以查看来源行或跳转到对应文档。
- 图表颜色、字体、背景和交互状态接入主题图表 token，不能在组件中固化一套颜色。
- 数据较大时先在服务层聚合，避免把全部属性视图数据直接交给 ECharts。

#### 架构方向

将“数据源—字段映射—聚合转换—图表呈现”拆开。数据库图表是第一个使用者，未来统计组件、Agent 临时工作台和外部文献组件也可以复用同一可视化管线。

### 12. 文献阅读数据组件与思源同步

> 用户当前称为 Rotelo 文献阅读软件；具体产品名称、可用 API 和授权方式在实现前确认。若实际目标为 Zotero，则按 Zotero 的正式数据接口和本地能力设计。

#### 产品定位

这是一个独立的文献数据集成和展示组件，不把第三方 MCP 直接安装到当前 Agent，也不让 Agent 负责文献库管理。

#### 展示能力

- 文献总数、分类/集合数量和分类分布。
- 最近阅读、最近新增、最近修改的文献。
- 文献的批注、笔记数量和最近笔记摘要。
- 阅读趋势、来源年份、作者、标签和文献类型等统计。
- 点击文献后打开来源软件、查看详情或打开已同步的思源文档。

#### 思源同步

- 将文献元数据、摘要、批注和笔记同步到用户指定的思源笔记本或父文档。
- 每条外部文献保存稳定来源 ID 与思源文档 ID 映射，保证重复同步不会重复创建。
- 明确同步方向；第一阶段建议以外部文献库到思源为主，不贸然做双向编辑。
- 支持同步预览、手动同步、增量同步、冲突提示、失败重试和同步记录。
- 用户可以选择文档模板、字段映射、笔记组织方式和是否同步附件链接。

#### 架构边界

- 外部连接、授权、读取、缓存和同步逻辑属于独立文献服务，不写在主页组件内部。
- 主页组件只消费标准化的文献摘要数据和同步状态。
- 后续可以把只读查询能力注册给 Agent，但这不是第一阶段目标，也不能成为组件可用的前提。

### 13. 内容对象右键动作

- 方向可以考虑，但需要结合每个组件内部的对象模型逐步接入，不要求一次覆盖所有组件。
- 第一批优先考虑最近文档、收藏文档、任务、任务管理 Plus、最近日记和复习文档。
- 组件通过对象适配器暴露稳定 ID、对象类型、标题、来源和支持动作。
- 普通右键优先处理内容对象；`Alt + 右键`继续作为强制打开组件菜单的统一方式。
- 动作由业务模块注册，例如打开、收藏、转为任务、加入今日计划、开始专注、交给 Agent、复制链接。
- 自定义文档和便签等编辑器区域不强行接入对象菜单，继续尊重编辑器自身右键行为。

## 明确暂缓或不考虑

### 插件自建全局命令面板

- 思源已有命令面板，插件再实现一套价值有限。
- 高频功能已有快捷键，熟练用户的主要问题不是缺少新的命令入口。
- 如果以后需要增强发现性，优先把主页功能注册到思源现有命令体系。

### 桌面端布局编辑模式增强

- 当前不作为近期开发方向。
- 项目已经有布局模板，多选、对齐、等宽等增强目前收益不足。
- 不新增常驻悬浮按钮或破坏默认页面清爽度的编辑控件。

### 单独建设全局快速预览与就地处理体系

- 当前预览框已经内置编辑器，用户可以直接查看和编辑文档。
- 重新建设一套统一快速预览的真实新增场景有限，暂不作为独立产品方向。
- 后续只针对现有预览框出现的实际问题做增强，不为了统一形式重写。

### 以手工规则配置为核心的自动化中心

- 跨组件自动化有价值，但不优先让用户配置大量触发器、条件和动作。
- 自动化优先围绕 Agent 定时任务、心跳任务、事件任务和自然语言配置展开。
- 底层仍需要确定性的调度和动作注册，但这些复杂性不必直接暴露给普通用户。

### 上下文镜头/场景过滤系统

- 同一布局为工作、学习、阅读等场景逐个配置数据过滤条件，配置成本较高。
- 预计愿意完整配置的用户有限，当前投入产出比不足。
- 后续如果 Agent 可以自动生成并维护场景配置，再重新评估。

## 其他组件高级化建议（尚待确认）

### 复习文档 → 复习中心

- 从简单复习计划升级为每日复习队列、逾期积压和周期统计。
- 支持按文档重要度、复习结果和遗忘程度调整下次复习时间。
- 记录“记得、模糊、忘记”等结果，展示保留率、积压趋势和最薄弱主题。
- 后续可让 Agent 基于文档生成复习问题，但复习计划本身保持确定性。

### 快速便签/快速记录 → 统一收集箱

- 将未处理的便签和快速记录集中为收集箱，而不是长期堆在多个组件里。
- 支持转换为文档、任务、习惯、日记记录或归档。
- Agent 可以辅助分类和提出转换建议，但用户确认后才执行写操作。

### 任务管理 Plus → 项目驾驶舱

- 将任务、子任务、项目进度、截止时间、专注时长和阻塞状态关联起来。
- 增加项目健康度、长期未推进任务、即将到期事项和工作量分布。
- 与全局日历、番茄钟和 Agent 临时工作台共享同一任务/项目数据服务。

### 纪念日/倒计时 → 里程碑中心

- 允许里程碑关联项目、任务和阶段完成度，而不只是显示剩余天数。
- 展示当前阶段、已完成事项、下一关键节点和时间风险。
- 仍保留纯纪念日模式，不强迫所有倒计时进入项目体系。

### 统计卡片、热力图、可视化图表 → 统一分析能力

- 统一数据源、日期范围、筛选、聚合和主题图表 token。
- 主页小组件负责摘要，完整数据中心负责钻取和比较。
- 让数据库图表、习惯、番茄钟、任务和文献模块复用同一分析管线，避免每个组件重新实现日期筛选和图表样式。

### 记账与固定资产 → 个人财务中心

- 在现有流水和资产基础上增加预算、周期账单、账户趋势和净资产变化。
- 主页只展示关键异常和摘要，完整分析进入财务中心。
- 此项尚未由用户确认优先级，不能挤占已经明确的 AI、日历、习惯和番茄钟方向。

## 建议实施顺序

该顺序同时考虑用户明确意愿和技术依赖，不代表必须一次全部完成：

1. 完成 P0 基础：设置搜索、组件呈现契约和性能观测。
2. 建设 Agent P0 内核：统一 Profile/错误协议、上下文清单、执行检查点和第一批真实失败 eval。
3. 用 10/30/100 轮上下文、工具故障和写操作最终状态评测验证 Agent；同步完成执行状态与恢复 UI。
4. 建设结构化个人记忆及候选、整理、冲突、确认、检索、遗忘生命周期。
5. 将对话、状态语、选中文字和机器人接入统一 Profile、诊断和权限协议。
6. 抽取强化日记领域服务，让主页摘要、完整工作台和 Agent 共用同一底层。
7. 建设主页 Agent 能力注册层，以及今日简报、建议收件箱和临时智能工作台第一阶段。
8. 建设 Agent 后台 Job 模型，以及一次性定时任务、周期任务和低成本心跳。
9. 建设统一日历事件提供者，并实现全局日历第一阶段。
10. 建设习惯领域数据模型和今日打卡组件。
11. 扩展现有专注会话，完成任务/项目/习惯绑定与专注数据中心。
12. 重构数据库图表的数据源和转换管线后重新开放。
13. 确认目标文献软件及接口，开发独立文献服务、展示组件和单向思源同步。
14. 按组件逐步增加内容对象动作，并评估其他组件高级化候选。

LLM Wiki / AI 知识管家已从当前实施顺序移除，只作为未来候选保留决策记录。
